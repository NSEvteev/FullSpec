# /platform/monitoring/ — Мониторинг

| IN | OUT |
|----|-----|
| prometheus/, grafana/, loki/ | Код логирования (→ `/src/`) |

## Структура

- `prometheus/` — сбор метрик, алерты
- `grafana/` — дашборды
- `loki/` — сбор логов
