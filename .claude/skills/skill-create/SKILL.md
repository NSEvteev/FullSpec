---
name: skill-create
description: Создание нового скилла по шаблону
allowed-tools: Bash, Write, Read, Edit, Glob, Grep
category: skill-management
triggers:
  commands:
    - /skill-create
  phrases:
    ru:
      - создай скилл
      - новый скилл
      - добавь скилл
      - создать скилл
      - сделай скилл
    en:
      - create skill
      - new skill
      - add skill
---

# Создание скилла

Команда для создания нового скилла по шаблону.

**Связанные скиллы:**
- [skill-update](/.claude/skills/skill-update/SKILL.md) — обновление *(не создан)*
- [skill-delete](/.claude/skills/skill-delete/SKILL.md) — удаление *(не создан)*

## Оглавление

- [Формат вызова](#формат-вызова)
- [Правила](#правила)
  - [Правило одного действия](#правило-одного-действия)
  - [Формат названия](#формат-названия)
  - [Скрипты Python](#скрипты-python)
  - [Ссылки на документы](#ссылки-на-документы)
- [Воркфлоу](#воркфлоу)
  - [Шаг 0: Проверка пересечений](#шаг-0-проверка-пересечений)
  - [Шаг 1: Получить название](#шаг-1-получить-название)
  - [Шаг 2: Категория](#шаг-2-категория)
  - [Шаг 3: Метаданные](#шаг-3-метаданные)
  - [Шаг 4: Создать файлы](#шаг-4-создать-файлы)
  - [Шаг 5: Валидация](#шаг-5-валидация)
  - [Шаг 6: Связь с агентами](#шаг-6-связь-с-агентами)
  - [Шаг 7: Обновить индекс](#шаг-7-обновить-индекс)
  - [Шаг 8: Синхронизация ссылок](#шаг-8-синхронизация-ссылок)
  - [Шаг 9: Результат](#шаг-9-результат)
- [Обработка ошибок](#обработка-ошибок)
- [Чек-лист создания скилла](#чек-лист-создания-скилла)
- [Шаблон SKILL.md](#шаблон-skillmd)
- [Примеры использования](#примеры-использования)

---

## Формат вызова

```
/skill-create [название]
```

## Правила

### Правило одного действия

**Скилл = 1 действие над объектом.**

| Действие | Описание |
|----------|----------|
| `create` | Создание нового объекта |
| `update` | Обновление существующего |
| `delete` | Удаление объекта |

Если пользователь просит создать скилл "для управления X" (подразумевая create + update + delete) — **разделить на 3 скилла**, объяснив правило.

### Формат названия

```
{объект}-{действие}
```

- Только латиница, цифры, дефис
- Примеры: `skill-create`, `agent-update`, `doc-delete`

### Скрипты Python

Повторяемые операции (создание файлов, поиск, валидация) выносить в `/.claude/scripts/`.

**Требование:** Если скилл использует Python-скрипт, в разделе "Воркфлоу" скилла **обязательно** описать:
1. Когда вызывается скрипт (на каком шаге)
2. Какие аргументы передаются
3. Что возвращает скрипт
4. Как обрабатывается результат

**Пример описания в воркфлоу:**

> ### Шаг 4: Создать файлы
>
> Вызвать скрипт:
> ```bash
> python .claude/scripts/skill_create.py --name {название} --category {категория}
> ```
>
> Аргументы:
> - `--name` — название скилла
> - `--category` — категория
>
> Результат: создаёт папку и SKILL.md, возвращает путь к файлу.

### Ссылки на документы

**Правило:** Все `.md` файлы, упоминаемые в тексте документа, должны быть оформлены как ссылки.

**Неправильно:**
```
Добавить скилл в /.claude/instructions/tools/skills.md
```

**Правильно:**
```
Добавить скилл в [skills.md](/.claude/instructions/tools/skills.md)
```

**Связанные скиллы:**
- `links-create` — создание ссылок в документе *(не создан)*
- `links-update` — обновление ссылок в связанных документах *(не создан)*

## Воркфлоу

### Шаг 0: Проверка пересечений

1. Прочитать индекс скиллов: [skills.md](/.claude/instructions/tools/skills.md)
2. Сравнить описание нового скилла с существующими
3. Если есть пересечение:
   - Изучить пересекающиеся скиллы
   - Вывести итог пользователю
   - Спросить: продолжить / отменить / изменить название

### Шаг 1: Получить название

1. Из аргумента: `/skill-create foo-create`
2. Или спросить: "Как назвать скилл?"
3. Валидация:
   - Формат `{объект}-{действие}`
   - Допустимые символы: `a-z`, `0-9`, `-`
   - Действие: `create`, `update`, `delete`

Если пользователь хочет несколько действий — разделить на отдельные скиллы.

### Шаг 2: Категория

1. Показать список существующих категорий из [skills.md](/.claude/instructions/tools/skills.md)
2. Предложить подходящую категорию
3. Если пользователь хочет новую — он должен явно сказать "новая категория: {название}"

**Текущие категории:**
- `skill-management` — управление скиллами
- `agent-management` — управление агентами
- `documentation` — документация
- `git` — git операции

### Шаг 3: Метаданные

1. Сгенерировать метаданные на основе названия и категории
2. Вывести на подтверждение пользователю
3. Если пользователь хочет изменить — скорректировать

| Поле | Описание |
|------|----------|
| `description` | Краткое описание (1 строка) |
| `allowed-tools` | Необходимые инструменты |
| `triggers.commands` | Команды для вызова |
| `triggers.phrases` | Фразы для активации (ru/en) |

**Пример вывода на подтверждение:**
```
Метаданные скилла agent-create:

description: Создание нового агента по шаблону
allowed-tools: Bash, Write, Read, Edit, Glob, Grep
triggers:
  commands: /agent-create
  phrases (ru): создай агента, новый агент
  phrases (en): create agent, new agent

Подтвердить? (да / изменить)
```

**Справочник allowed-tools:**
- `Bash` — выполнение команд
- `Read` — чтение файлов
- `Write` — создание файлов
- `Edit` — редактирование файлов
- `Glob` — поиск файлов по паттерну
- `Grep` — поиск в содержимом файлов
- `WebFetch` — запросы к URL
- `WebSearch` — поиск в интернете
- `Task` — запуск подзадач

### Шаг 4: Создать файлы

```bash
mkdir -p .claude/skills/{название}
```

Создать `SKILL.md` по шаблону (см. ниже).

Если нужны скрипты Python — создать в `/.claude/scripts/{название}.py`.

### Шаг 5: Валидация

Проверить созданный SKILL.md по чек-листу:

**Структура:**
- [ ] Есть frontmatter с обязательными полями (name, description, allowed-tools, category, triggers)
- [ ] Есть связанные скиллы в начале (после описания)
- [ ] Есть оглавление со ссылками
- [ ] Есть описание назначения
- [ ] Есть раздел "Правила" (если применимо)
- [ ] Есть воркфлоу с шагами
- [ ] Есть чек-лист (если нужен)
- [ ] Есть минимум 1 пример использования

**Вёрстка:**
- [ ] Frontmatter — корректный YAML
- [ ] Ссылки в оглавлении соответствуют заголовкам
- [ ] Нет вложенных блоков кода (``` внутри ```)
- [ ] Таблицы форматированы корректно
- [ ] Все блоки кода закрыты
- [ ] Все `.md` файлы оформлены как ссылки

**Ссылки:**
- [ ] Вызвать скилл `links-create` для создания ссылок *(когда будет создан)*

Если отклонения — предупредить (не блокировать).

### Шаг 6: Связь с агентами

1. Прочитать [agents.md](/.claude/instructions/tools/agents.md)
2. Найти агентов с тегами, соответствующими категории скилла
3. Предложить: "Скилл `{название}` относится к `{категория}`. Назначить агенту `{агент}`?"
4. Если да — добавить скилл в `skills:` агента

### Шаг 7: Обновить индекс

1. Добавить скилл в [skills.md](/.claude/instructions/tools/skills.md):
   - В таблицу соответствующей категории
   - С описанием и триггерами

2. Найти связанные скиллы (тот же объект, другое действие):
   - Например, для `doc-create` связанные: `doc-update`, `doc-delete`
   - Добавить ссылки в раздел "Связанные скиллы" (если есть)

### Шаг 8: Синхронизация ссылок

Обновить связанные документы, чтобы они знали о новом скилле.

1. Вызвать скилл `links-update` *(когда будет создан)*:
   - Скрипт находит все `.md` файлы, связанные с созданным скиллом
   - Проходит по каждому файлу
   - Добавляет информацию о новом скилле (ссылки, упоминания)

2. Файлы для обновления:
   - [skills.md](/.claude/instructions/tools/skills.md) — добавить в индекс
   - [agents.md](/.claude/instructions/tools/agents.md) — если назначен агенту
   - Связанные скиллы (`{объект}-update`, `{объект}-delete`) — добавить ссылку

3. Подтвердить обновление:
   ```
   Обновлены связанные документы:
   - skills.md — добавлен в категорию {категория}
   - agent-name.md — добавлен в skills
   - {объект}-update/SKILL.md — добавлена ссылка
   ```

### Шаг 9: Результат

Вывести полный отчёт:
```
✅ Скилл создан успешно

Файлы:
- .claude/skills/{название}/SKILL.md

Метаданные:
- Категория: {категория}
- Описание: {description}
- Триггеры: /{название}, "{фраза1}", "{фраза2}"

Связи:
- Агенты: {список или "не назначен"}
- Связанные скиллы: {список или "нет"}

Индекс обновлён: /.claude/instructions/tools/skills.md

Следующие шаги:
- Проверить и доработать инструкции в SKILL.md
- Добавить примеры использования (если нужно больше)
- Протестировать скилл
```

---

## Обработка ошибок

Если на любом шаге произошла ошибка:

1. **Сообщить пользователю:**
   ```
   ❌ Ошибка создания скилла на шаге {N}: {описание ошибки}

   Процесс не завершён. Откатываю изменения...
   ```

2. **Откатить все изменения:**
   - Удалить созданную папку `.claude/skills/{название}/`
   - Удалить созданный скрипт `.claude/scripts/{название}.py` (если был)
   - Убрать запись из `skills.md` (если была добавлена)
   - Убрать скилл из `skills:` агента (если был добавлен)

3. **Подтвердить откат:**
   ```
   Изменения откачены. Можно попробовать снова.
   ```

**Типичные ошибки:**
- Скилл с таким названием уже существует
- Невалидный формат названия
- Не найден файл `skills.md` или `agents.md`
- Пользователь отменил операцию

---

## Чек-лист создания скилла

Использовать при выполнении скилла:

- [ ] **Шаг 0:** Прочитал [skills.md](/.claude/instructions/tools/skills.md), проверил пересечения
- [ ] **Шаг 1:** Получил название, проверил формат `{объект}-{действие}`
- [ ] **Шаг 1:** Если несколько действий — разделил на отдельные скиллы
- [ ] **Шаг 2:** Показал категории, получил выбор пользователя
- [ ] **Шаг 3:** Сгенерировал метаданные, вывел на подтверждение
- [ ] **Шаг 4:** Создал папку `.claude/skills/{название}/`
- [ ] **Шаг 4:** Создал `SKILL.md` по шаблону
- [ ] **Шаг 4:** Если нужен скрипт — создал в `.claude/scripts/`, описал флоу в воркфлоу
- [ ] **Шаг 5:** Проверил структуру SKILL.md (чек-лист валидации)
- [ ] **Шаг 5:** Проверил вёрстку (YAML, ссылки, блоки кода)
- [ ] **Шаг 5:** Проверил, что все `.md` файлы оформлены как ссылки
- [ ] **Шаг 6:** Прочитал [agents.md](/.claude/instructions/tools/agents.md), предложил назначить агентам по тегам
- [ ] **Шаг 6:** Если назначен — обновил `skills:` в агенте
- [ ] **Шаг 7:** Добавил скилл в [skills.md](/.claude/instructions/tools/skills.md) в таблицу категории
- [ ] **Шаг 7:** Нашёл связанные скиллы, добавил ссылки (если есть)
- [ ] **Шаг 8:** Вызвал `links-update` для синхронизации связанных документов
- [ ] **Шаг 9:** Вывел полный отчёт пользователю

---

## Шаблон SKILL.md

~~~markdown
---
name: {название}
description: {описание}
allowed-tools: {инструменты}
category: {категория}
triggers:
  commands:
    - /{название}
  phrases:
    ru:
      - {фраза 1}
      - {фраза 2}
    en:
      - {phrase 1}
      - {phrase 2}
---

# {Заголовок}

{Краткое описание назначения скилла — 1-2 предложения.}

**Связанные скиллы:**
- [{объект}-update](/.claude/skills/{объект}-update/SKILL.md) — обновление
- [{объект}-delete](/.claude/skills/{объект}-delete/SKILL.md) — удаление

## Оглавление

{Ссылки на разделы}

---

## Формат вызова

```
/{название} [аргументы]
```

## Правила

{Специфичные правила для этого скилла, если есть.}

## Воркфлоу

### Шаг 1: {Название}

{Описание действий}

### Шаг 2: {Название}

{Описание действий}

---

## Чек-лист

{Если нужен}

---

## Примеры использования

### Пример 1: {Сценарий}

**Команда:**
```
/{название} {аргументы}
```

**Результат:**
{Описание результата}
~~~

---

## Примеры использования

### Пример 1: Создание скилла для агентов

**Вызов:**
```
/skill-create agent-create
```

**Диалог:**
```
> Категория: предлагаю "agent-management". Подходит?
Да

> Описание скилла:
Создание нового агента по шаблону

> Скилл относится к управлению агентами. Назначить агенту для управления агентами (если появится)?
Да

Создан скилл: .claude/skills/agent-create/SKILL.md
Категория: agent-management
Добавлен в индекс: /.claude/instructions/tools/skills.md
```

### Пример 2: Попытка создать универсальный скилл

**Вызов:**
```
Создай скилл для управления документацией
```

**Ответ:**
```
По правилам, скилл выполняет только 1 действие над объектом.

Создам 3 скилла:
- doc-create — создание документа
- doc-update — обновление документа
- doc-delete — удаление документа

Продолжить?
```

### Пример 3: Обнаружено пересечение

**Вызов:**
```
/skill-create file-create
```

**Ответ:**
```
Обнаружено возможное пересечение:

Существующий скилл: doc-create
Описание: Создание нового документа
Пересечение: оба создают файлы

Варианты:
1. Продолжить создание file-create (разные области применения)
2. Использовать существующий doc-create
3. Изменить название нового скилла

Что выбрать?
```
