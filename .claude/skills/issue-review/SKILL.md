---
name: issue-review
description: Ревью решения перед закрытием Issue
allowed-tools: Read, Bash, Edit, Glob, Grep
category: git
triggers:
  commands:
    - /issue-review
  phrases:
    ru:
      - проверь решение
      - ревью задачи
      - оцени реализацию
    en:
      - review solution
      - review issue
      - check implementation
---

# Ревью решения

Команда для проверки качества решения перед закрытием Issue. Автоматически вызывается после `/issue-execute`.

**Связанные скиллы:**
- [issue-create](/.claude/skills/issue-create/SKILL.md) — создание Issue
- [issue-update](/.claude/skills/issue-update/SKILL.md) — обновление Issue
- [issue-execute](/.claude/skills/issue-execute/SKILL.md) — взятие Issue в работу и выполнение
- [issue-complete](/.claude/skills/issue-complete/SKILL.md) — закрытие Issue как выполненного
- [issue-delete](/.claude/skills/issue-delete/SKILL.md) — закрытие Issue как неактуального

**Связанные инструкции:**
- [git/issues.md](/.claude/instructions/git/issues.md) — правила работы с Issues
- [git/workflow.md](/.claude/instructions/git/workflow.md) — правила работы с ветками и PR
- [git/commits.md](/.claude/instructions/git/commits.md) — формат сообщений коммитов
- [git/review.md](/.claude/instructions/git/review.md) — правила code review

## Оглавление

- [Формат вызова](#формат-вызова)
- [Когда использовать](#когда-использовать)
- [Правила](#правила)
  - [Критерии ревью](#критерии-ревью)
  - [Оценка качества](#оценка-качества)
- [Воркфлоу](#воркфлоу)
  - [Шаг 1: Получить контекст](#шаг-1-получить-контекст)
  - [Шаг 2: Проверить качество кода](#шаг-2-проверить-качество-кода)
  - [Шаг 3: Оценить реализацию](#шаг-3-оценить-реализацию)
  - [Шаг 4: Проверить тесты](#шаг-4-проверить-тесты)
  - [Шаг 5: Проверить критерии готовности](#шаг-5-проверить-критерии-готовности)
  - [Шаг 6: Сформировать отчёт](#шаг-6-сформировать-отчёт)
  - [Шаг 7: Применить исправления](#шаг-7-применить-исправления-если-есть)
  - [Шаг 8: Принять решение](#шаг-8-принять-решение)
  - [Шаг 9: Проверка по чек-листу](#шаг-9-проверка-по-чек-листу)
- [Обработка ошибок](#обработка-ошибок)
- [Чек-лист](#чек-лист)
- [Примеры использования](#примеры-использования)

---

## Формат вызова

```
/issue-review [номер | --last] [--auto] [--strict]
```

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `номер` | Номер Issue | — |
| `--last` | Использовать последний взятый Issue | false |
| `--auto` | Автоматический режим (вызван из issue-execute) | false |
| `--strict` | Строгий режим (любое замечание = блокировка) | false |

**Примеры:**
- `/issue-review 123` — ревью Issue #123
- `/issue-review --last` — ревью последнего Issue из `.claude/state/last-issue-run.json`
- `/issue-review 123 --auto` — автоматический режим после issue-execute
- `/issue-review 123 --strict` — строгий режим

### Флаг --last

При использовании `--last` скилл читает номер Issue из файла состояния:

```bash
cat .claude/state/last-issue-run.json | jq '.issue'
```

Если файл состояния не существует:
```
⚠️ Файл состояния не найден: .claude/state/last-issue-run.json

Укажите номер Issue явно:
/issue-review {номер}
```

## Когда использовать

| Ситуация | Действие |
|----------|----------|
| После `/issue-execute` | Вызывается автоматически |
| Перед созданием PR | Проверить качество решения |
| После доработок | Повторное ревью |

---

## Правила

### Критерии ревью

**Правило:** Ревью проверяет 4 аспекта решения.

| Аспект | Что проверяется | Блокирует |
|--------|-----------------|:---------:|
| Качество кода | Линтинг, типизация, стиль | ✅ |
| Оптимальность | Можно ли реализовать лучше | ⚠️ |
| Тесты | Тесты проходят, покрытие | ✅ |
| Критерии готовности | Все пункты из Issue выполнены | ✅ |

> **Дополнительно:** Для полной проверки качества используй [чек-лист self-review](/.claude/instructions/git/review.md#чек-лист-self-review-для-автора) из инструкции review.md.

**Легенда:**
- ✅ — блокирует закрытие при провале
- ⚠️ — предупреждение, решение за пользователем

### Оценка качества

**Правило:** При оценке "можно ли реализовать лучше" проверять:

| Критерий | Вопрос |
|----------|--------|
| Читаемость | Понятен ли код без комментариев? |
| Простота | Нет ли избыточной сложности? |
| Переиспользование | Используются ли существующие утилиты? |
| Производительность | Нет ли очевидных проблем? |
| Безопасность | Нет ли уязвимостей? |

---

## Воркфлоу

### Шаг 1: Получить контекст

1. Получить информацию об Issue:
   ```bash
   gh issue view {номер} --json title,body,labels
   ```

2. Найти изменённые файлы:
   ```bash
   git diff main --name-only
   ```

3. Определить scope ревью:
   ```
   📋 Ревью Issue #{номер}

   Заголовок: {title}
   Изменённые файлы: {count}

   Файлы:
   - src/auth/handlers.ts
   - src/auth/utils.ts
   - tests/auth/handlers.test.ts
   ```

### Шаг 2: Проверить качество кода

1. Запустить линтер (если настроен):
   ```bash
   npm run lint
   # или
   make lint
   ```

2. Проверить типы (если TypeScript):
   ```bash
   npm run typecheck
   # или
   npx tsc --noEmit
   ```

3. Результат:
   ```
   🔍 Качество кода

   Линтинг: ✅ Пройден (0 ошибок, 2 предупреждения)
   Типизация: ✅ Пройдена
   ```

   Или при ошибках:
   ```
   🔍 Качество кода

   Линтинг: ❌ 3 ошибки
   - src/auth/handlers.ts:45 — unused variable
   - src/auth/handlers.ts:67 — missing return type
   - src/auth/utils.ts:12 — any type

   Типизация: ❌ 1 ошибка
   - src/auth/handlers.ts:45 — Type 'string' is not assignable to 'number'
   ```

### Шаг 3: Оценить реализацию

**Правило:** Прочитать изменённый код, собрать ВСЕ предложения по улучшению, показать списком.

1. Прочитать каждый изменённый файл
2. Оценить по критериям из [Оценка качества](#оценка-качества)
3. **Собрать все предложения** (НЕ применять по одному):

   ```
   💡 Предложения по улучшению

   Найдено предложений: 3

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   [1] 📄 src/auth/handlers.ts:45-52

   Текущий код:
   ```typescript
   const result = [];
   for (const item of items) {
     if (item.active) {
       result.push(item.name);
     }
   }
   ```

   Предложение:
   ```typescript
   const result = items.filter(item => item.active).map(item => item.name);
   ```

   Причина: Более читаемый функциональный стиль

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   [2] 📄 src/auth/utils.ts:12

   Текущий код:
   ```typescript
   if (token === null || token === undefined) {
   ```

   Предложение:
   ```typescript
   if (!token) {
   ```

   Причина: Упрощение проверки

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   [3] 📄 src/auth/handlers.ts:78

   Текущий код:
   ```typescript
   console.log('Debug:', data);
   ```

   Предложение: Удалить отладочный вывод

   Причина: Не должно быть в продакшн-коде

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Применить:
   [A] Все  [1,2,3] Выборочно  [N] Пропустить все
   ```

4. При согласии — применить выбранные через `Edit`

5. После применения:
   ```
   💡 Оценка реализации: ✅ Улучшено (3 изменения)
   ```
   или при отсутствии предложений:
   ```
   💡 Оценка реализации: ✅ Оптимально
   ```

### Шаг 4: Проверить тесты

1. Запустить тесты:
   ```bash
   npm test
   # или
   make test
   ```

2. Результат:
   ```
   🧪 Тесты

   Статус: ✅ Все тесты пройдены (24 passed)
   Покрытие: 85% (порог: 80%)
   ```

   Или при ошибках:
   ```
   🧪 Тесты

   Статус: ❌ 2 теста провалены

   FAIL src/auth/handlers.test.ts
     ✕ should validate token (45ms)
     ✕ should refresh expired token (23ms)

   Исправить и повторить? [Y/n]
   ```

3. При `Y` — попытаться исправить тесты, затем повторить

### Шаг 5: Проверить критерии готовности

1. Извлечь критерии из body Issue (чек-лист)
2. Проверить каждый пункт:

   ```
   📋 Критерии готовности

   - [x] Реализована авторизация через Google
   - [x] Реализована авторизация через GitHub
   - [x] Написаны тесты
   - [ ] Обновлена документация ⚠️

   Не выполнено: 1 пункт
   ```

3. Если есть невыполненные:
   ```
   ⚠️ Не все критерии выполнены

   Невыполненный пункт: "Обновлена документация"

   Варианты:
   [1] Выполнить сейчас
   [2] Пропустить (критерий неактуален)
   [3] Прервать ревью
   ```

### Шаг 6: Сформировать отчёт

```
═══════════════════════════════════════
📋 ОТЧЁТ РЕВЬЮ — Issue #{номер}
═══════════════════════════════════════

🔍 Качество кода:     ✅ Пройдено
💡 Оптимальность:     ✅ Улучшено (2 изменения)
🧪 Тесты:             ✅ Пройдены (24/24)
📋 Критерии:          ✅ Выполнены (4/4)

═══════════════════════════════════════
ИТОГ: ✅ РЕВЬЮ ПРОЙДЕНО
═══════════════════════════════════════
```

Или при проблемах:

```
═══════════════════════════════════════
📋 ОТЧЁТ РЕВЬЮ — Issue #{номер}
═══════════════════════════════════════

🔍 Качество кода:     ❌ 3 ошибки линтинга
💡 Оптимальность:     ⚠️ 1 предложение отклонено
🧪 Тесты:             ❌ 2 теста провалены
📋 Критерии:          ⚠️ 1 не выполнен

═══════════════════════════════════════
ИТОГ: ❌ РЕВЬЮ НЕ ПРОЙДЕНО

Блокирующие проблемы:
1. Ошибки линтинга (3)
2. Провалены тесты (2)

Следующие шаги:
- Исправить ошибки линтинга
- Починить провалившиеся тесты
- Запустить /issue-review 123 повторно
═══════════════════════════════════════
```

### Шаг 7: Применить исправления (если есть)

**Правило:** Если на шагах 2-5 были найдены и согласованы исправления кода (НЕ документации) — применить их сразу и вернуться к Шагу 2.

**Алгоритм:**

1. Проверить, были ли согласованы изменения:
   - Шаг 2: ошибки линтинга/типов
   - Шаг 3: предложения по улучшению
   - Шаг 4: исправления тестов

2. Если изменения есть И это НЕ документация:
   ```
   🔄 Применяю исправления...

   Применено:
   - src/auth/handlers.ts — 2 изменения
   - src/auth/utils.ts — 1 изменение

   Возвращаюсь к проверке качества кода...
   ```

   **→ Перейти к Шагу 2** (повторная проверка)

3. Если изменения только в документации:
   ```
   📝 Изменения в документации требуют ручной проверки.

   Файлы:
   - doc/auth/README.md

   Применить? [Y/n]
   ```

4. Если изменений нет или все применены и проверка пройдена:
   ```
   ✅ Все проверки пройдены, изменений больше нет.
   ```

   **→ Перейти к Шагу 8**

### Шаг 8: Принять решение

**Если ревью пройдено (все проверки ✅):**

```
✅ Ревью пройдено. Вызываю /issue-complete {номер}...
```

Вызвать [/issue-complete](/.claude/skills/issue-complete/SKILL.md):

```
/issue-complete {номер}
```

**Если ревью не пройдено (есть ❌):**

```
❌ Ревью не пройдено

Issue #{номер} остаётся открытым.
Исправьте проблемы и запустите /issue-review {номер} повторно.
```

НЕ вызывать issue-complete.

### Шаг 9: Проверка по чек-листу

```
✅ Шаг 1: Получил контекст (Issue, файлы)
✅ Шаг 2: Проверил качество кода
✅ Шаг 3: Оценил реализацию, собрал предложения
✅ Шаг 4: Проверил тесты
✅ Шаг 5: Проверил критерии готовности
✅ Шаг 6: Сформировал отчёт
✅ Шаг 7: Применил исправления (цикл к Шагу 2 при необходимости)
✅ Шаг 8: Принял решение (complete / reject)
```

> **Дополнительная проверка:** [Чек-лист self-review](/.claude/instructions/git/review.md#чек-лист-self-review-для-автора) — критерии качества перед отправкой на review.

---

## Обработка ошибок

| Ошибка | Действие |
|--------|----------|
| Issue не найден | Сообщить: "Issue #{num} не найден" |
| Issue закрыт | Сообщить: "Issue #{num} уже закрыт" |
| Нет изменённых файлов | Предупредить: "Нет изменений для ревью" |
| Линтер не настроен | Пропустить проверку, предупредить |
| Тесты не настроены | Пропустить проверку, предупредить |
| Нет критериев в Issue | Спросить подтверждение выполнения |

---

## Чек-лист

- [ ] **Шаг 1:** Получил информацию об Issue
- [ ] **Шаг 1:** Определил изменённые файлы
- [ ] **Шаг 2:** Запустил линтер (если есть)
- [ ] **Шаг 2:** Проверил типизацию (если есть)
- [ ] **Шаг 3:** Прочитал изменённый код
- [ ] **Шаг 3:** Собрал ВСЕ предложения по улучшению
- [ ] **Шаг 3:** Показал список предложений пользователю
- [ ] **Шаг 4:** Запустил тесты
- [ ] **Шаг 4:** Проверил покрытие (если есть порог)
- [ ] **Шаг 5:** Извлёк критерии готовности из Issue
- [ ] **Шаг 5:** Проверил каждый критерий
- [ ] **Шаг 6:** Сформировал итоговый отчёт
- [ ] **Шаг 7:** Применил согласованные исправления (если есть)
- [ ] **Шаг 7:** Если код изменён — вернулся к Шагу 2
- [ ] **Шаг 8:** Вызвал `/issue-complete` (если пройдено)
- [ ] **Шаг 9:** Проверил выполнение чек-листа

---

## Примеры использования

### Пример 1: Успешное ревью с улучшениями

**Вызов:**
```
/issue-review 123
```

**Диалог:**
```
📋 Ревью Issue #123

Заголовок: [AUTH] Добавить OAuth авторизацию
Изменённые файлы: 4

Файлы:
- src/auth/oauth.ts
- src/auth/handlers.ts
- tests/auth/oauth.test.ts
- tests/auth/handlers.test.ts

Начинаю проверку...

🔍 Качество кода
Линтинг: ✅ Пройден
Типизация: ✅ Пройдена

💡 Предложения по улучшению

Найдено предложений: 2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1] 📄 src/auth/oauth.ts:23-28

Текущий код:
```typescript
if (token === null || token === undefined) {
  throw new Error('No token');
}
```

Предложение:
```typescript
if (!token) {
  throw new Error('No token');
}
```

Причина: Упрощение проверки

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[2] 📄 src/auth/handlers.ts:45

Текущий код:
```typescript
const users = data.users ? data.users : [];
```

Предложение:
```typescript
const users = data.users ?? [];
```

Причина: Использование nullish coalescing

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Применить:
[A] Все  [1,2] Выборочно  [N] Пропустить все
> A

🔄 Применяю исправления...

Применено:
- src/auth/oauth.ts — 1 изменение
- src/auth/handlers.ts — 1 изменение

Возвращаюсь к проверке качества кода...

🔍 Качество кода
Линтинг: ✅ Пройден
Типизация: ✅ Пройдена

💡 Оценка реализации: ✅ Оптимально (новых предложений нет)

🧪 Тесты
Статус: ✅ Все тесты пройдены (18 passed)
Покрытие: 87%

📋 Критерии готовности
- [x] Реализована авторизация через Google
- [x] Реализована авторизация через GitHub
- [x] Написаны тесты

═══════════════════════════════════════
📋 ОТЧЁТ РЕВЬЮ — Issue #123
═══════════════════════════════════════

🔍 Качество кода:     ✅ Пройдено
💡 Оптимальность:     ✅ Улучшено (2 изменения)
🧪 Тесты:             ✅ Пройдены (18/18)
📋 Критерии:          ✅ Выполнены (3/3)

═══════════════════════════════════════
ИТОГ: ✅ РЕВЬЮ ПРОЙДЕНО
═══════════════════════════════════════

✅ Ревью пройдено. Вызываю /issue-complete 123...
```

### Пример 2: Ревью с ошибками

**Вызов:**
```
/issue-review 124
```

**Диалог:**
```
📋 Ревью Issue #124

Заголовок: [NOTIFY] Исправить отправку email
Изменённые файлы: 2

Начинаю проверку...

🔍 Качество кода
Линтинг: ❌ 2 ошибки

src/notify/email.ts:34 - 'response' is defined but never used
src/notify/email.ts:56 - Missing return type on function

Исправить? [Y/n]
> Y

Исправляю...
✅ Ошибки исправлены

🧪 Тесты
Статус: ❌ 1 тест провален

FAIL src/notify/email.test.ts
  ✕ should send email with attachment (234ms)
    Expected: 200
    Received: 500

Исправить? [Y/n]
> Y

Анализирую причину...

Проблема: Не обрабатывается случай пустого attachment.
Исправляю src/notify/email.ts:78...

✅ Тест исправлен

Перезапускаю тесты...
🧪 Тесты: ✅ Все тесты пройдены (12/12)

📋 Критерии готовности
- [x] Email отправляется
- [x] Вложения работают
- [x] Есть retry при ошибке

═══════════════════════════════════════
ИТОГ: ✅ РЕВЬЮ ПРОЙДЕНО
═══════════════════════════════════════

✅ Ревью пройдено. Вызываю /issue-complete 124...
```

### Пример 3: Ревью не пройдено

**Вызов:**
```
/issue-review 125 --strict
```

**Диалог:**
```
📋 Ревью Issue #125

Заголовок: [PAY] Интеграция с платёжной системой
Изменённые файлы: 6

Начинаю проверку (строгий режим)...

🔍 Качество кода
Линтинг: ⚠️ 1 предупреждение

src/pay/stripe.ts:89 - Unexpected any. Specify a different type.

В строгом режиме предупреждения блокируют.

Исправить? [Y/n]
> n

❌ Проверка качества кода не пройдена

═══════════════════════════════════════
ИТОГ: ❌ РЕВЬЮ НЕ ПРОЙДЕНО
═══════════════════════════════════════

Блокирующие проблемы:
1. Предупреждение линтера (строгий режим)

Issue #125 остаётся открытым.
Исправьте проблемы и запустите /issue-review 125 повторно.
```

### Пример 4: Автоматический режим (после issue-execute)

**Контекст:** Вызван автоматически из issue-execute.

**Вызов:**
```
/issue-review 123 --auto
```

**Диалог:**
```
📋 Автоматическое ревью Issue #123

[Все проверки проходят без интерактива]
[Улучшения применяются автоматически если очевидны]
[Неочевидные улучшения пропускаются]

═══════════════════════════════════════
ИТОГ: ✅ РЕВЬЮ ПРОЙДЕНО
═══════════════════════════════════════

✅ Ревью пройдено. Вызываю /issue-complete 123...
```

---

## FAQ / Troubleshooting

### Когда использовать режим --strict?

**Ответ:** Используйте `--strict` когда:
- Код будет смержен в main/master
- Изменения критичны для продакшена
- Требуется высокое качество кода

**Эффект:** Все предупреждения становятся блокирующими ошибками.

### Линтер не настроен — что делать?

**Симптом:** "Линтер не найден, пропускаю проверку"

**Решения:**

| Язык | Команда настройки |
|------|-------------------|
| TypeScript | `npm init @eslint/config` |
| Python | `pip install flake8` или `ruff` |
| Go | Встроен: `go vet` |

**Временное решение:** Ревью пройдёт без проверки линтера, но с предупреждением.

### Как пропустить проверку тестов?

**Ответ:** Нельзя пропустить в нормальном режиме.

**Если тесты не применимы:**
1. Добавить комментарий в Issue: "Тесты не применимы"
2. При вопросе "Исправить тесты?" ответить `n`
3. Подтвердить пропуск проверки

### Ревью зацикливается — что делать?

**Симптом:** После применения исправлений снова появляются те же проблемы.

**Причины:**
1. Автоформатирование конфликтует с линтером
2. Циклическая зависимость в правилах

**Решение:**
1. Исправить конфигурацию линтера/форматтера
2. Применить изменения вручную
3. Запустить `/issue-review --auto` для упрощённой проверки

### Как повторить ревью после исправлений?

**Команда:**
```
/issue-review {номер}
```

**Что произойдёт:**
1. Все проверки запустятся заново
2. Ранее применённые улучшения учтутся
3. Новые проблемы (если есть) будут показаны

### Ревью пройдено, но issue-complete не вызван — почему?

**Причины:**
1. Есть блокирующие предупреждения (в режиме --strict)
2. Критерии готовности не выполнены
3. Ошибка при вызове gh

**Решение:** Вызвать вручную:
```
/issue-complete {номер}
```
