# Тесты: agent-create

## TC-AGENT-CREATE-001: Создание агента с валидным названием

**Шаги:**
1. Вызвать `/agent-create docs-agent --auto`

**Ожидаемый результат:**
- Создан файл `/.claude/agents/docs-agent.md`
- Файл содержит корректный frontmatter (name, description, tags, skills)
- Агент добавлен в таблицу `agents.md`

**Статус:** pending

---

## TC-AGENT-CREATE-002: Агент уже существует

**Предусловие:**
- Существует `/.claude/agents/test-agent.md`

**Шаги:**
1. Вызвать `/agent-create test-agent`

**Ожидаемый результат:**
- Выведено предупреждение "Агент test-agent уже существует"
- Предложены варианты: перезаписать / отменить

**Статус:** pending

---

## TC-AGENT-CREATE-003: Невалидное название

**Шаги:**
1. Вызвать `/agent-create Invalid Name`

**Ожидаемый результат:**
- Ошибка валидации
- Показаны правила именования
- Агент не создан

**Статус:** pending

---

## TC-AGENT-CREATE-004: Режим --dry-run

**Шаги:**
1. Вызвать `/agent-create new-agent --dry-run`

**Ожидаемый результат:**
- Показан план создания
- Файлы НЕ созданы
- Индекс НЕ обновлён

**Статус:** pending

---

## TC-AGENT-CREATE-005: Выбор скиллов по тегам

**Шаги:**
1. Вызвать `/agent-create git-helper`
2. Выбрать тег `git`

**Ожидаемый результат:**
- Предложены скиллы из категории git (issue-*)
- Выбранные скиллы добавлены в frontmatter агента

**Статус:** pending

---

## TC-AGENT-CREATE-006: Обновление индекса agents.md

**Шаги:**
1. Вызвать `/agent-create test-agent --auto`
2. Проверить `/.claude/agents/README.md`

**Ожидаемый результат:**
- В таблице "Список агентов" появилась новая строка
- Ссылка на агента корректна
- Теги и скиллы указаны

**Статус:** pending
