# Комплексный анализ документации проекта

**Дата:** 2026-01-20
**Статус:** ✅ Завершён

---

## Статистика проекта

| Метрика | Значение |
|---------|----------|
| **Всего файлов в /.claude/** | 143 |
| **Размер** | 2.1 МБ |
| **Инструкций** | 53 (100% создано) |
| **Скиллов** | 37 |
| **Тестов для скиллов** | 37 |
| **Шаблонов** | 6 |
| **Агентов** | 0 (папка пуста) |

---

## Ответы на вопросы

### 1. Дублирование файлов ✅

**Статус:** ✅ Исправлено

---

### 2. Качество описания ✅

**Статус:** ✅ Исправлено

---

### 3. Неотвеченные вопросы

**Выявленные пробелы:**

| Вопрос | Где должен быть ответ | Статус |
|--------|----------------------|--------|
| Как создать новый сервис? | `/doc/src/` или инструкция | ❌ Нет |
| Как связать ADR с кодом? | `doc/structure.md` | ❌ Нет |
| Какой workflow документации? | `doc/workflow.md` | ❌ Нет |
| Как работают дискуссии → архитектура → ADR? | Был в старой системе | ❌ Утрачен |
| Что такое "агент" и зачем они? | `/.claude/agents/README.md` | ⚠️ Частично |
| Как запустить Claude-тесты? | `tests/claude-testing.md` | ✅ Есть |

---

### 4. Связи между документами

**Статус:** ✅ Хорошая связанность

**SSOT файлы (критичные):**
- `instructions/README.md` — индекс 53 инструкций
- `/.claude/skills/README.md` — каталог 37 скиллов
- `/.claude/README.md` — граф зависимостей
- `templates/output-formats.md` — ссылается из 15+ скиллов

**Несвязанные:**
- `/.claude/agents/README.md` — нет агентов
- `shared/assets.md` — нет примеров
- `doc/structure.md` — не ссылается на workflow

---

### 5. Переиспользование сущностей

**Возможности:**

| Сущность | Где может использоваться |
|----------|-------------------------|
| Шаблоны `templates/` | При создании документации в `/doc/` |
| Скиллы doc-* | Для автоматизации `/doc/` |
| SSOT scope-detection.md | В любом новом скилле |
| Контракты `/shared/contracts/` | Генерация API docs в `/doc/` |
| Инструкции `/src/api/` | Валидация при code review |

**Что не переиспользуется:**
- Инструкции не связаны со скиллами явно (только 43% имеют раздел "Скиллы")
- Шаблоны из `templates/` не упоминаются в `doc-create`

---

### 6. Контекстные связи

**Текущий граф:**

```
CLAUDE.md (точка входа)
    ↓
/.claude/instructions/README.md (индекс)
    ↓
├── /src/** → правила для /src/
├── /platform/** → правила для /platform/
├── /tests/** → правила для /tests/
└── /tools/** → skills, agents, dependencies

/.claude/skills/ (37 скиллов)
    ↓
├── Вызывают друг друга по графу зависимостей
├── Ссылаются на instructions/
└── Используют templates/ как SSOT
```

**Что можно улучшить:**
1. Связать `/doc/structure.md` с doc-* скиллами
2. Связать git/* инструкции с issue-* скиллами (частично сделано)
3. Добавить `/doc/glossary.md` и связать через glossary-* скиллы

---

### 7. Улучшения workflow скиллов

**Найденные проблемы:**

| Проблема | Скиллы | Решение |
|----------|--------|---------|
| Нет примеров | links-create, links-delete, prompt-update | Добавить 3-4 примера |
| Слишком короткие | skill-report (69 строк), issue-reopen (146 строк) | Расширить до 500+ строк |
| Нет agent-update, agent-delete | Упоминаются как "планируются" | Создать |
| Высокая нагрузка на links-update | Вызывается из 10+ скиллов | Документировать edge cases |

**Рекомендуемые улучшения:**

1. **Унифицировать формат** — все SKILL.md минимум 500 строк
2. **Добавить валидацию** — проверка состояния перед выполнением
3. **Parallel execution** — skill-create + instruction-create параллельно где возможно
4. **Rollback mechanism** — автооткат при ошибке на любом шаге

---

### 8. Новые скиллы для создания

**Приоритет 1 (критично):**

| Скилл | Назначение | Почему нужен |
|-------|-----------|--------------|
| `/discuss-create` | Создание дискуссии | Workflow документации (утрачен) |
| `/architecture-create` | Создание архитектуры | Workflow документации (утрачен) |
| `/adr-create` | Создание ADR | Workflow документации (утрачен) |
| `/glossary-update` | Управление глоссарием | Единая терминология (утрачен) |
| `/agent-update` | Обновление агента | Симметрия с agent-create |
| `/agent-delete` | Удаление агента | Симметрия с agent-create |

**Приоритет 2 (желательно):**

| Скилл | Назначение |
|-------|-----------|
| `/service-create` | Создание нового сервиса в /src/ |
| `/doc-feedback` | Каскадное обновление при изменении |
| `/doc-index` | Автообновление индексов 000_*.md |
| `/plan-create` | Создание плана реализации |

---

### 9. Тесты для Claude

**Текущее состояние:**
- 37 скиллов имеют tests.md (100%)
- Методология в `claude-testing.md`
- Форматы в `test-formats.md`

**Что нужно добавить:**

| Тип теста | Что тестировать | Приоритет |
|-----------|-----------------|-----------|
| **E2E workflow** | discuss → architecture → ADR → plan | 🔴 Высокий |
| **Cross-skill** | skill-create + links-update + skill-update | 🔴 Высокий |
| **Error recovery** | Откат при ошибке на середине | 🟡 Средний |
| **Parallel execution** | Параллельные issue-* | 🟡 Средний |
| **Integration** | Claude + GitHub API (issue-*) | 🟢 Низкий |

---

### 10. Общая структура папок проекта

**КРИТИЧНО:** Текущая структура описана в CLAUDE.md и README.md, но НЕ СОЗДАНА физически.

**Предложение — создать папку `/project-template/` со структурой:**

```
/project-template/
├── .claude/
│   ├── agents/
│   ├── discussions/
│   ├── instructions/       # Копируется из текущего
│   ├── scripts/
│   ├── skills/             # Копируется из текущего
│   ├── state/
│   └── templates/          # Копируется из текущего
│
├── config/
│   ├── development.yaml.example
│   ├── staging.yaml.example
│   └── production.yaml.example
│
├── doc/
│   ├── README.md
│   ├── glossary.md
│   ├── 01_discuss/         # ← ВОССТАНОВИТЬ из старой системы
│   ├── 02_architecture/    # ← ВОССТАНОВИТЬ
│   ├── 03_diagrams/        # ← ВОССТАНОВИТЬ
│   ├── 04_adr/             # ← ВОССТАНОВИТЬ
│   └── src/                # Зеркало /src/
│
├── platform/
│   ├── docker/
│   ├── gateway/
│   └── monitoring/
│
├── shared/
│   ├── contracts/
│   ├── events/
│   └── libs/
│
├── src/
│   └── {service-template}/
│       ├── backend/
│       ├── database/
│       └── tests/
│
├── tests/
│   ├── e2e/
│   ├── integration/
│   └── load/
│
├── CLAUDE.md
├── Makefile
└── README.md
```

---

### 11. Summary/навигация для агентов ✅

**Статус:** ✅ Исправлено

**Проблема:** Пользователь путается в документации.

**Решение — создать иерархию навигации:**

```
УРОВЕНЬ 1: CLAUDE.md
├── Что это за проект?
├── Как начать работу?
└── Ссылки на уровень 2

УРОВЕНЬ 2: /.claude/README.md (СОЗДАТЬ)
├── Как работать со скиллами?
├── Как работать с инструкциями?
├── Как работать с документацией?
└── Быстрый поиск по задаче

УРОВЕНЬ 3: Индексы папок
├── /.claude/instructions/README.md — все инструкции
├── /.claude/skills/README.md (СОЗДАТЬ) — все скиллы с кратким описанием
├── /doc/README.md — как работать с документацией
└── /src/README.md (СОЗДАТЬ) — структура сервисов
```

**Формат Summary для агентов:**

```markdown
# Summary для агента {имя}

## Твоя зона ответственности
- {папки}
- {типы файлов}

## Какие скиллы использовать
| Задача | Скилл |
|--------|-------|
| ... | /... |

## Где искать правила
- Инструкции: /.claude/instructions/{область}/
- Шаблоны: /.claude/templates/

## Важные ограничения
- ...
```

---

### 12. Workflow документации

**Текущее состояние:** ❌ УТРАЧЕН при миграции

**Был в старой системе (llm_instructions_old + .claude_old):**

```
Дискуссия (01_discuss/)
    ↓ draft → in_progress → review → approved → final
    ↓
Архитектура (02_architecture/)
    ↓ draft → in_progress → review → approved → final
    ↓
ADR (04_decisions/)
    ↓ draft → review → approved
    ↓
Ресурсы (05_resources/)
    ↓ database/ backend/ frontend/ infra/
    ↓
План реализации (06_imp_plans/)
    ↓ draft → in_progress → review → test → approved → final
    ↓
Код (/src/)
```

**Скиллы для workflow (были):**
- `discussion` — создание дискуссии
- `discussion-review` — ревью варианта
- `summary-doc` — агрегация решений
- `architect` — создание архитектуры
- `architect-review` — ревью архитектуры
- `summary-arch` — агрегация решений
- `decision` — создание ADR
- `resource` — создание ресурса
- `imp-plan` — создание плана
- `feedback` — каскадное обновление

**Что восстановить:**

1. **Структура /doc/**
   ```
   /doc/
   ├── 01_discuss/           # Дискуссии
   ├── 02_architecture/      # Архитектура
   ├── 03_diagrams/          # Диаграммы
   ├── 04_adr/               # ADR
   ├── 05_resources/         # Ресурсы
   ├── 06_plans/             # Планы
   └── src/                  # Документация сервисов
   ```

2. **Инструкции**
   - `/.claude/instructions/doc/discuss.md`
   - `/.claude/instructions/doc/architecture.md`
   - `/.claude/instructions/doc/adr.md`
   - `/.claude/instructions/doc/workflow.md` — общий workflow

3. **Скиллы**
   - `/discuss-create`, `/discuss-review`
   - `/architecture-create`, `/architecture-review`
   - `/adr-create`
   - `/plan-create`
   - `/doc-feedback`

4. **Агент**
   - `/.claude/agents/documentation-manager.md` — аналог Amy Santiago

---

## План действий

### Фаза 1: Устранение критичных проблем (1-2 дня)

| Задача | Статус |
|--------|--------|
| Разделить testing.md | ✅ |
| Заполнить agents.md | ✅ |
| Исправить placeholder CVE в security.md | ✅ |
| Создать /.claude/README.md | ✅ |
| Расширить skill-report | ⏳ |
| Расширить issue-reopen | ⏳ |

### Фаза 2: Восстановление workflow (3-5 дней)

| Задача | Действие |
|--------|----------|
| Создать структуру /doc/ | mkdir |
| Создать инструкции doc/*.md | /instruction-create × 5 |
| Создать скиллы discuss-*, architecture-*, adr-* | /skill-create × 8 |
| Создать агента documentation-manager | Ручное |

### Фаза 3: Улучшения (1-2 недели)

| Задача | Действие |
|--------|----------|
| Добавить раздел "Скиллы" в 30 инструкций | Edit |
| Заполнить TODO в platform/* | Edit |
| Создать шаблоны документов | Write |
| Создать глоссарий | Write |

---

## Ключевые выводы

1. ✅ **Проект хорошо структурирован** — 53 инструкции, 37 скиллов, 100% создано
2. ⏳ **Утрачен важный workflow** — система документирования из старой версии
3. ✅ **Навигация создана** — README.md во всех подпапках /.claude/
4. ⏳ **Физическая структура не создана** — только описана в README.md
5. ✅ **Дублирование исправлено** — зоны ответственности разделены

---

## Файлы-источники

| Файл | Назначение |
|------|------------|
| `/CLAUDE.md` | Точка входа |
| `/README.md` | Описание структуры |
| `/.claude/instructions/README.md` | Индекс инструкций |
| `/.claude/skills/README.md` | Индекс скиллов |
| `/llm_instructions_old/` | Старая система (для восстановления) |
| `/.claude_old/` | Старые скиллы (для восстановления) |
