---
description: Стандарт формата скиллов (сокращённый формат)
standard: .instructions/standard-instruction.md
index: .claude/.instructions/skills/README.md
---

# Стандарт скиллов

Формат и структура файлов SKILL.md в сокращённом формате.

**Полезные ссылки:**
- [Инструкции для скиллов](./README.md)

## Оглавление

- [1. Назначение](#1-назначение)
- [2. Расположение](#2-расположение)
- [3. Формат файла](#3-формат-файла)
- [4. Frontmatter](#4-frontmatter)
- [5. Секции документа](#5-секции-документа)
- [6. Примеры](#6-примеры)

---

## 1. Назначение

**Скилл = триггер + ссылка на SSOT-инструкцию.**

Скилл НЕ содержит логику — он только:
- Определяет команду и фразы для вызова
- Указывает на SSOT-инструкцию с деталями
- Описывает формат вызова и параметры

**Когда создавать скилл:**
- Есть SSOT-инструкция с воркфлоу
- Нужен удобный триггер для вызова
- Действие повторяется

**Когда НЕ создавать скилл:**
- Нет SSOT-инструкции (сначала создать инструкцию!)
- Разовое действие
- Простое действие без воркфлоу

**Принцип:**
```
1. Инструкция (SSOT)     ← Сначала
2. Скилл (триггер)       ← Потом
```

---

## 2. Расположение

```
/.claude/skills/{skill-name}/SKILL.md
```

### Правила именования

| Правило | Пример ✅ | Пример ❌ |
|---------|----------|----------|
| Формат `{object}-{action}` | `structure-create` | `create-structure` |
| Kebab-case | `skill-create` | `skillCreate` |
| Латиница | `links-validate` | `ссылки-валидация` |

### Категории скиллов

| Категория | Описание | Примеры |
|-----------|----------|---------|
| `documentation` | Работа с документацией | structure-*, spec-* |
| `code` | Работа с кодом | — |
| `testing` | Тестирование | — |
| `deployment` | Деплой | — |

### Правило одного действия

Скилл выполняет **1 действие** над объектом:
- `create` — создание
- `update` — обновление
- `delete` — удаление
- `validate` — валидация
- `migrate` — миграция

---

## 3. Формат файла

### Принцип сокращённого формата

```
┌─────────────────────────────────────┐
│ Frontmatter (триггеры, метаданные)  │
├─────────────────────────────────────┤
│ **SSOT:** ссылка на инструкцию      │
├─────────────────────────────────────┤
│ Формат вызова (параметры)           │
├─────────────────────────────────────┤
│ Воркфлоу: "→ Выполнить из SSOT"     │
├─────────────────────────────────────┤
│ Чек-лист: ссылка на SSOT            │
├─────────────────────────────────────┤
│ Примеры                             │
└─────────────────────────────────────┘
```

### Размер

| Формат | Строк |
|--------|-------|
| Сокращённый (новый) | 40-60 |
| Старый | 100-300 |

### Ключевое отличие

| Аспект | Старый формат | Сокращённый формат |
|--------|---------------|-------------------|
| Шаги воркфлоу | Дублируют инструкцию | Ссылка на SSOT |
| Чек-лист | Дублирует | Ссылка на SSOT |
| Обновление | В 2 местах | Только в инструкции |

---

## 4. Frontmatter

```yaml
---
name: {skill-name}
description: {Краткое описание — одно предложение}
allowed-tools: Read, Bash, Glob, Grep, Write, Edit
category: documentation
triggers:
  commands:
    - /{skill-name}
  phrases:
    ru:
      - {фраза на русском}
      - {альтернативная фраза}
    en:
      - {phrase in english}
---
```

### Обязательные поля

| Поле | Описание | Пример |
|------|----------|--------|
| `name` | Имя скилла (kebab-case) | `structure-create` |
| `description` | Описание (1 строка) | `Создание новой папки` |
| `allowed-tools` | Разрешённые инструменты | `Read, Bash, Glob` |
| `category` | Категория | `documentation` |
| `triggers.commands` | Команды для вызова | `- /structure-create` |
| `triggers.phrases` | Фразы (ru/en) | `- создай папку` |

### Справочник allowed-tools

| Инструмент | Описание |
|------------|----------|
| `Bash` | Выполнение команд в терминале |
| `Read` | Чтение файлов |
| `Write` | Создание файлов |
| `Edit` | Редактирование файлов |
| `Glob` | Поиск файлов по паттерну |
| `Grep` | Поиск в содержимом файлов |

---

## 5. Секции документа

### Обязательные секции

| Секция | Содержание |
|--------|------------|
| **SSOT** | Ссылка на инструкцию (сразу после заголовка) |
| **Формат вызова** | Синтаксис + таблица параметров |
| **Воркфлоу** | Предупреждение + "→ Выполнить шаги из SSOT" |
| **Чек-лист** | Ссылка на чек-лист в SSOT |
| **Примеры** | 2-3 примера вызова |

### Шаблон секций

```markdown
# {Название}

**SSOT:** [{инструкция}.md]({путь к инструкции})

## Формат вызова

```
/{skill-name} <параметр> [--опция]
```

| Параметр | Описание | Обязательный |
|----------|----------|--------------|
| `<параметр>` | Описание | Да |
| `--опция` | Описание | Нет |

## Воркфлоу

> ⚠️ **Перед выполнением** прочитать [{инструкция}.md]({путь})

→ Выполнить шаги из SSOT-инструкции.

## Чек-лист

→ См. [{инструкция}.md#чек-лист]({путь}#чек-лист)

## Примеры

```
/{skill-name} example1
/{skill-name} example2 --option value
```
```

### Стандартные флаги

| Флаг | Описание | Тип |
|------|----------|-----|
| `--dry-run` | Показать план без выполнения | boolean |
| `--auto` | Автоматический режим без подтверждений | boolean |

---

## 6. Примеры

### Пример: structure-create

```markdown
---
name: structure-create
description: Создание новой папки в структуре проекта
allowed-tools: Read, Bash, Glob, Grep, Write, Edit
category: documentation
triggers:
  commands:
    - /structure-create
  phrases:
    ru:
      - создай папку
      - добавь папку
    en:
      - create folder
---

# Создание папки

**SSOT:** [create-structure.md](/.structure/.instructions/create-structure.md)

## Формат вызова

```
/structure-create <путь> [--description "Описание"]
```

| Параметр | Описание | Обязательный |
|----------|----------|--------------|
| `<путь>` | Путь к новой папке | Да |
| `--description` | Описание для SSOT | Нет (спросит) |

## Воркфлоу

> ⚠️ **Перед выполнением** прочитать [create-structure.md](/.structure/.instructions/create-structure.md)

→ Выполнить шаги из SSOT-инструкции.

## Чек-лист

→ См. [create-structure.md#чек-лист](/.structure/.instructions/create-structure.md#чек-лист)

## Примеры

```
/structure-create docs --description "Документация проекта"
/structure-create src/utils --description "Утилиты"
```
```

---

## Связанные инструкции

- [validation-skill.md](./validation-skill.md) — валидация скиллов
- [create-skill.md](./create-skill.md) — создание скилла
- [modify-skill.md](./modify-skill.md) — изменение скилла
