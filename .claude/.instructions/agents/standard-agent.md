---
description: Стандарт формата и правил для агентов Claude
standard: .instructions/standard-instruction.md
index: .claude/.instructions/agents/README.md
---

# Стандарт агентов

Формат конфигураций, типы и правила написания промптов для агентов Claude.

**Полезные ссылки:**
- [Инструкции agents](./README.md)
- [Claude Code окружение](../../README.md)

**Связанные документы:**

| Тип | Документ |
|-----|----------|
| Стандарт | Этот документ |
| Валидация | [validation-agent.md](./validation-agent.md) |
| Создание | [create-agent.md](./create-agent.md) |
| Модификация | [modify-agent.md](./modify-agent.md) |

## Оглавление

- [1. Назначение агентов](#1-назначение-агентов)
- [2. Типы агентов](#2-типы-агентов)
- [3. Формат конфигурации](#3-формат-конфигурации)
  - [Структура файла](#структура-файла)
  - [Обязательные поля](#обязательные-поля)
  - [Опциональные поля](#опциональные-поля)
- [4. Правила написания промптов](#4-правила-написания-промптов)
  - [Структура промпта](#структура-промпта)
  - [Принципы](#принципы)
  - [Антипаттерны](#антипаттерны)
- [5. Контекст для агента](#5-контекст-для-агента)
  - [Обязательные данные](#обязательные-данные)
  - [Скиллы и правила](#скиллы-и-правила)
- [6. Примеры промптов](#6-примеры-промптов)
  - [Explore-агент: поиск TODO](#explore-агент-поиск-todo)
  - [Plan-агент: анализ архитектуры](#plan-агент-анализ-архитектуры)
  - [General-purpose: код-ревью](#general-purpose-код-ревью)
- [7. Расположение и именование](#7-расположение-и-именование)
- [8. Обработка ошибок](#8-обработка-ошибок)
- [9. Отмена и прерывание](#9-отмена-и-прерывание)
- [10. Безопасность](#10-безопасность)

---

## 1. Назначение агентов

Агенты — автономные помощники для выполнения специализированных задач через инструмент `Task`.

**Когда создавать агента:**

| Критерий | Порог |
|----------|-------|
| Повторяемость задачи | > 3 раз |
| Специализация | Узкая область (explore, validate, build) |
| Автономность | Задача выполнима без постоянного участия пользователя |

**Когда НЕ создавать агента:**

- Разовая задача → выполнить напрямую
- Требует постоянного диалога → использовать основной контекст
- Простая команда → использовать Bash

---

## 2. Типы агентов

| Тип | Назначение | Инструменты | Пример |
|-----|------------|-------------|--------|
| **Explore** | Исследование кодовой базы | Glob, Grep, Read | Поиск паттернов, анализ структуры |
| **Bash** | Выполнение команд | Bash | Git операции, сборка, тесты |
| **Plan** | Планирование реализации | Read, Glob, Grep | Архитектурные решения |
| **general-purpose** | Универсальные задачи | Все инструменты | Сложные многошаговые задачи |

**Встроенные агенты Claude Code:**

```
Explore      — быстрый поиск по кодовой базе
Bash         — выполнение shell-команд
Plan         — планирование реализации
general-purpose — универсальный агент
```

**Кастомные агенты:**

Создаются в `/.claude/agents/` для специфичных задач проекта.

---

## 3. Формат конфигурации

### Структура файла

```yaml
# /.claude/agents/{agent-name}.yaml

name: agent-name
description: Краткое описание (одно предложение)
type: explore | bash | plan | general-purpose

prompt: |
  {Системный промпт агента}

tools:
  - Glob
  - Grep
  - Read

skills:                           # Для general-purpose агентов
  - /structure-validate
  - /links-validate
  - /principles-validate

settings:
  model: haiku | sonnet | opus
  max_turns: 20                   # Зависит от сложности задачи
```

### Обязательные поля

| Поле | Тип | Описание |
|------|-----|----------|
| `name` | string | Уникальное имя агента (kebab-case) |
| `description` | string | Краткое описание назначения |
| `type` | enum | Тип агента (см. секцию 2) |
| `prompt` | string | Системный промпт |

### Опциональные поля

| Поле | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `tools` | list | Зависит от type | Доступные инструменты |
| `skills` | list | [] | Доступные скиллы (для general-purpose) |
| `settings.model` | enum | sonnet | Модель для агента |
| `settings.max_turns` | int | — | Максимум API-вызовов (итераций) |

**О `max_turns`:**

`max_turns` ограничивает количество итераций агента (вызовов API). Это предохранитель от зацикливания.

| Тип задачи | Рекомендуемое значение |
|------------|------------------------|
| Простой поиск | 5-10 |
| Анализ папки | 10-20 |
| Код-ревью | 15-30 |
| Сложная задача | 30-50 |
| Без ограничения | не указывать (или 100+) |

> **Принцип:** Лучше указать с запасом. Агент остановится раньше, если закончит задачу.

---

## 4. Правила написания промптов

### Процесс формирования промпта

> **Промпт формируется итеративно с подтверждением пользователя.**

**Шаги (выполняются при создании агента):**

1. **Сбор контекста** — LLM автоматически:
   - Получает список инструкций: `python .instructions/.scripts/list-instructions.py`
   - Анализирует релевантные инструкции для задачи агента
   - Определяет, какие скиллы могут понадобиться

2. **Генерация черновика промпта** — на основе:
   - Запроса пользователя
   - Найденных инструкций и стандартов
   - Рекомендуемых скиллов

3. **Подтверждение пользователем** — AskUserQuestion:
   - "Промпт корректен?" → Да / Нужны правки
   - При правках: уточнить что исправить, вернуться к шагу 2

4. **Выбор скиллов** — если агент general-purpose:
   - LLM предлагает список скиллов на основе задачи
   - Пользователь выбирает, какие разрешить

> **Детали процесса:** см. [create-agent.md](./create-agent.md)

### Структура промпта

```markdown
## Роль
{Кто агент и какова его специализация}

## Задача
{Что агент должен делать}

## Инструкции и SSOT
{Ссылки на инструкции, которым следует агент}

## Доступные скиллы
{Список скиллов, если применимо}

## Ограничения
{Чего агент НЕ должен делать}

## Формат вывода
{Как агент должен оформлять результат}
```

### Принципы

> **Специфичность.** Промпт описывает конкретную задачу, не общие инструкции.

> **Ограничения явные.** Что агент НЕ должен делать — важнее того, что должен.

> **Формат вывода.** Всегда указывать ожидаемый формат результата.

**Хороший промпт:**

```yaml
prompt: |
  ## Роль
  Ты — агент для поиска TODO-комментариев в коде.

  ## Задача
  Найти все TODO, FIXME, HACK комментарии в указанной папке.

  ## Ограничения
  - НЕ модифицировать файлы
  - НЕ предлагать исправления
  - Только поиск и отчёт

  ## Формат вывода
  Таблица: | Файл | Строка | Комментарий |
```

### Антипаттерны

| Антипаттерн | Пример ❌ | Исправление ✅ |
|-------------|----------|----------------|
| Слишком общий | "Помоги с кодом" | "Найди неиспользуемые импорты в src/" |
| Без ограничений | "Исправь баги" | "Найди баги, НЕ исправляй автоматически" |
| Без формата | "Проанализируй" | "Выведи список в формате markdown" |

---

## 5. Контекст для агента

### Обязательные данные

Для качественной работы агенту необходим полный контекст задачи.

**Чек-лист данных для передачи агенту:**

| Данные | Описание | Пример |
|--------|----------|--------|
| **Цель** | Что именно нужно сделать | "Найти все неиспользуемые функции" |
| **Область** | Где искать/работать | "src/services/", "*.py файлы" |
| **Критерии** | Как определить успех | "Функция не вызывается нигде" |
| **Исключения** | Что игнорировать | "tests/, __init__.py" |
| **Формат результата** | Как оформить вывод | "Таблица markdown" |
| **Ограничения** | Чего НЕ делать | "НЕ удалять, только отчёт" |

**Полный шаблон промпта для агента:**

```markdown
## Роль
{Кто агент: специализация, область экспертизы}

Пример: "Ты — агент для поиска технического долга в Python-коде."

## Задача
{Что конкретно должен сделать агент}

Пример: "Найти все TODO, FIXME, HACK комментарии и сгруппировать по приоритету."

## Область работы
{Где искать/работать, какие файлы/папки}

- Путь: {src/, tests/, конкретная папка}
- Типы файлов: {*.py, *.ts, *.md}
- Исключения: {node_modules/, .git/, __pycache__/}

## Инструкции и SSOT
{Ссылки на инструкции проекта, которым агент должен следовать}

Релевантные инструкции:
- /.instructions/standard-principles.md — принципы кода
- /.structure/.instructions/standard-readme.md — формат README

## Доступные скиллы
{Для general-purpose агентов — какие скиллы разрешено использовать}

При необходимости используй:
- /principles-validate — проверка принципов
- /links-validate — проверка ссылок
- /structure-validate — проверка структуры

## Правила проекта
{Агент следует тем же правилам, что и основной LLM}

Правила из /.claude/rules/:
- core.md — приоритет скиллов, SSOT, шаблоны

## Критерии успеха
{Как определить, что задача выполнена правильно}

- {критерий 1}
- {критерий 2}

## Ограничения
{Чего агент НЕ должен делать — критически важно}

- НЕ {ограничение 1}
- НЕ {ограничение 2}
- ВСЕГДА {обязательное поведение}

## Формат вывода
{Точный формат результата — таблица, список, markdown}

Пример структуры вывода:
| Колонка 1 | Колонка 2 | Колонка 3 |
|-----------|-----------|-----------|
| ...       | ...       | ...       |

Итого: {что должно быть в конце}
```

### Работа с файлами состояния

Агенты могут создавать файлы для сохранения промежуточных и итоговых результатов:

| Папка | Назначение | Пример |
|-------|------------|--------|
| `/.claude/state/` | Временные файлы между сессиями | `analysis-cache.json` |
| `/.claude/drafts/` | Черновики для больших задач | `refactoring-plan.md` |

**Когда создавать драфт:**

- Задача требует > 10 итераций
- Результат нужен основному LLM после завершения
- Анализ слишком большой для вывода в чат

**Пример использования:**

```yaml
prompt: |
  ## Сохранение результатов

  Для больших результатов:
  1. Создать драфт в /.claude/drafts/{task-name}.md
  2. Записать полный анализ в драфт
  3. Вернуть краткое резюме + путь к драфту

  Временные данные сохраняй в /.claude/state/
```

> **Принцип:** Драфт остаётся после завершения работы агента — основной LLM может его использовать.

### Координация между агентами

При запуске нескольких агентов параллельно им нужен контекст друг о друге.

**Передача информации о других агентах:**

```yaml
prompt: |
  ## Контекст параллельной работы

  Другие запущенные агенты:
  - todo-finder: ищет TODO в src/
  - unused-imports: анализирует импорты в src/

  Координация через /.claude/state/:
  - Читай state/agents-status.json для статуса других агентов
  - Записывай свой прогресс в state/{agent-name}-progress.json
  - Избегай конфликтов: не модифицируй файлы, которые обрабатывает другой агент
```

**Файл координации (state/agents-status.json):**

```json
{
  "agents": [
    {
      "name": "todo-finder",
      "status": "running",
      "area": "src/",
      "started": "2024-01-15T10:30:00Z"
    },
    {
      "name": "unused-imports",
      "status": "completed",
      "area": "src/",
      "result": "state/unused-imports-result.json"
    }
  ]
}
```

**Правила координации:**

| Правило | Описание |
|---------|----------|
| Избегать конфликтов | Не модифицировать файлы, над которыми работает другой агент |
| Сообщать о прогрессе | Записывать статус в state/ для видимости |
| Делиться результатами | Сохранять промежуточные результаты в state/ |
| Читать чужие результаты | Использовать данные других агентов если релевантно |

### Скиллы и правила

> **Агенты наследуют правила основного LLM.** Все rules из `/.claude/rules/` применяются к агентам автоматически.

**Автоматический сбор контекста при создании:**

При создании агента LLM ОБЯЗАН:

1. Выполнить поиск инструкций:
   ```bash
   python .instructions/.scripts/list-instructions.py
   ```

2. Проанализировать какие инструкции релевантны задаче агента

3. Включить ссылки на SSOT в промпт агента

4. Предложить список скиллов для разрешения (AskUserQuestion с multiSelect)

**Доступ к скиллам:**

Агенты типа `general-purpose` могут использовать скиллы. Указывайте доступные скиллы в промпте:

```yaml
prompt: |
  ## Доступные скиллы
  При необходимости используй скиллы:
  - /structure-validate — валидация структуры
  - /links-validate — проверка ссылок
  - /principles-validate — проверка принципов кода

  ## Правила
  Следуй правилам из /.claude/rules/:
  - core.md — приоритет скиллов, SSOT, шаблоны
```

**Когда назначать скиллы:**

| Задача агента | Рекомендуемые скиллы |
|---------------|---------------------|
| Валидация проекта | /structure-validate, /links-validate |
| Рефакторинг | /principles-validate |
| Создание файлов | /instruction-create, /skill-create, /structure-create |
| Изменение структуры | /structure-modify, /instruction-modify |

---

## 6. Примеры промптов

### Explore-агент: поиск TODO

```yaml
name: todo-finder
description: Поиск TODO/FIXME комментариев в кодовой базе
type: explore

prompt: |
  ## Роль
  Агент для поиска технического долга в коде.

  ## Задача
  Найти все комментарии с маркерами: TODO, FIXME, HACK, XXX, BUG.

  ## Контекст
  - Область поиска: указывается в запросе (по умолчанию — весь src/)
  - Языки: Python, TypeScript, JavaScript, Markdown

  ## Ограничения
  - НЕ модифицировать файлы
  - НЕ предлагать исправления в этом запуске
  - Только сбор и группировка

  ## Формат вывода
  Markdown таблица с группировкой по приоритету:
  - По маркерам: FIXME → TODO → HACK
  - По файлам: количество каждого типа

tools:
  - Glob
  - Grep
  - Read

settings:
  model: haiku
  max_turns: 5
```

### Plan-агент: анализ архитектуры

```yaml
name: architecture-analyzer
description: Анализ архитектуры и зависимостей модулей
type: plan

prompt: |
  ## Роль
  Архитектурный аналитик для оценки структуры проекта.

  ## Задача
  Проанализировать указанную область и построить карту зависимостей.

  ## Контекст для сбора
  1. Структура папок и их назначение
  2. Импорты между модулями
  3. Циклические зависимости
  4. Нарушения слоёв (presentation → data напрямую)

  ## Правила анализа
  Следуй принципам из /.instructions/standard-principles.md:
  - Модули должны зависеть от абстракций
  - Однонаправленный поток зависимостей
  - Разделение по слоям

  ## Ограничения
  - Только анализ, НЕ рефакторинг
  - НЕ создавать файлы

  ## Формат вывода
  Markdown отчёт с секциями:
  - Слои (Presentation, Business, Data)
  - Таблица проблем
  - Рекомендации

tools:
  - Glob
  - Grep
  - Read

settings:
  model: sonnet
  max_turns: 10
```

### General-purpose: код-ревью

```yaml
name: code-reviewer
description: Автоматический код-ревью с проверкой принципов
type: general-purpose

prompt: |
  ## Роль
  Код-ревьюер для проверки качества кода.

  ## Задача
  Проверить указанные файлы на соответствие стандартам проекта.

  ## Доступные скиллы
  Используй для автоматизации:
  - /principles-validate — проверка принципов программирования
  - /links-validate — проверка ссылок (для .md файлов)

  ## Правила проверки
  SSOT: /.instructions/standard-principles.md

  Проверяй:
  1. Именование (переменные, функции, классы)
  2. Структура модулей
  3. Обработка ошибок
  4. Документация (docstrings)
  5. Тестируемость

  ## Ограничения
  - НЕ автоисправлять код
  - Спрашивать перед любыми изменениями
  - Группировать замечания по серьёзности

  ## Формат вывода
  Таблица markdown с группировкой по серьёзности:
  - Критичные: блокирующие проблемы
  - Важные: требуют внимания
  - Минорные: рекомендации

settings:
  model: sonnet
  max_turns: 15
```

---

## 7. Расположение и именование

### Расположение

```
/.claude/agents/
├── {agent-name}.yaml       # Конфигурация агента
└── README.md               # Индекс агентов
```

### Именование

| Правило | Пример ✅ | Пример ❌ |
|---------|----------|----------|
| Kebab-case | `todo-finder.yaml` | `todoFinder.yaml` |
| Описательное | `unused-imports.yaml` | `cleaner.yaml` |
| Без префиксов | `test-runner.yaml` | `agent-test-runner.yaml` |
| Латиница | `code-review.yaml` | `проверка-кода.yaml` |

---

## 8. Обработка ошибок

Агент должен явно сообщать об ошибках и проблемах.

### Типы ошибок

| Тип | Действие агента | Пример |
|-----|-----------------|--------|
| **Блокирующая** | Остановиться, вернуть ошибку | Файл не найден, нет доступа |
| **Восстановимая** | Пропустить, продолжить | Один файл повреждён из 100 |
| **Предупреждение** | Записать, продолжить | Подозрительный паттерн |

### Формат сообщения об ошибке

```markdown
## Ошибка выполнения

**Тип:** {блокирующая/восстановимая}
**Шаг:** {на каком шаге произошла}
**Причина:** {описание проблемы}

**Контекст:**
- Файл: {путь}
- Операция: {что пытался сделать}

**Рекомендации:**
1. {что можно сделать}
```

### В промпте агента

```yaml
prompt: |
  ## Обработка ошибок

  При ошибках:
  1. Записать в state/{agent-name}-errors.json
  2. Продолжить если ошибка восстановимая
  3. Остановиться если блокирующая
  4. Вернуть отчёт с секцией "Ошибки"

  Формат записи:
  {
    "timestamp": "...",
    "type": "blocking|recoverable|warning",
    "message": "...",
    "context": {...}
  }
```

---

## 9. Отмена и прерывание

### Когда агент должен остановиться

| Ситуация | Действие |
|----------|----------|
| Критическая ошибка | Немедленная остановка + отчёт |
| Превышен max_turns | Остановка + промежуточный результат |
| Конфликт с другим агентом | Ожидание + уведомление (см. ниже) |
| Задача невыполнима | Остановка + объяснение |

### Конфликт с другим агентом

При обнаружении конфликта (файл занят другим агентом):

1. **Записать конфликт** в `state/conflicts.json`
2. **Уведомить основной LLM** через возврат сообщения
3. **Ожидать освобождения** — проверять `state/agents-status.json` каждые 5 секунд
4. **Проверить изменения** — прочитать что изменил другой агент
5. **Проверить совместимость** — убедиться что свои изменения не перезатрут чужие
6. **При конфликте изменений** — спросить основной LLM (он спросит пользователя)
7. **Продолжить** только после разрешения конфликта

```yaml
prompt: |
  ## При конфликте с другим агентом

  1. НЕ останавливаться сразу
  2. Записать в state/conflicts.json:
     {
       "agent": "{my-name}",
       "blocked_by": "{other-agent}",
       "file": "{путь}",
       "status": "waiting",
       "waiting_since": "timestamp",
       "resumed_at": null
     }
  3. Вернуть сообщение: "Ожидаю освобождения {файл} от {agent}"
  4. Проверять state/agents-status.json каждые 5 секунд
  5. Когда other-agent завершил:
     a. Прочитать изменения другого агента
     b. Сравнить с запланированными своими изменениями
     c. Если конфликт:
        - Вернуть сообщение основному LLM с описанием конфликта
        - Ждать решения пользователя
     d. Если нет конфликта — продолжить работу
  6. При продолжении работы обновить state/conflicts.json:
     - status: "resumed"
     - resumed_at: "timestamp"
  7. Вернуть сообщение: "Продолжаю работу после ожидания {время}"
  8. Таймаут ожидания: 2 минуты → остановка с ошибкой
```

**Формат сообщения о конфликте изменений:**

```markdown
## Конфликт изменений

**Файл:** {путь}

**Изменения агента {other-agent}:**
- {что изменил}

**Мои запланированные изменения:**
- {что хотел изменить}

**Конфликт:**
- {описание конфликта}

**Варианты:**
1. Применить мои изменения (перезаписать чужие)
2. Отменить мои изменения (оставить чужие)
3. Объединить вручную
```

### Graceful shutdown

```yaml
prompt: |
  ## При прерывании

  Если задача прервана:
  1. Сохранить текущий прогресс в state/{agent-name}-checkpoint.json
  2. Записать что уже сделано
  3. Записать что осталось
  4. Вернуть частичный результат

  Формат checkpoint:
  {
    "completed": ["шаг1", "шаг2"],
    "pending": ["шаг3"],
    "partial_result": {...}
  }
```

### Запрос на отмену от пользователя

Агент должен периодически проверять возможность прерывания (через state/).

```yaml
prompt: |
  ## Проверка отмены

  Каждые 5 итераций проверяй state/cancel-requests.json
  Если есть запрос на отмену этого агента — выполнить graceful shutdown
```

---

## 10. Безопасность

### Запрещённые операции

| Операция | Почему запрещено |
|----------|------------------|
| `rm -rf /` | Уничтожение системы |
| Удаление .git | Потеря истории |
| Модификация .env | Утечка секретов |
| Push без подтверждения | Необратимое действие |
| Удаление node_modules в продакшене | Поломка сервиса |

### Обязательные ограничения в промпте

```yaml
prompt: |
  ## Ограничения безопасности

  ЗАПРЕЩЕНО без явного разрешения пользователя:
  - Удалять файлы (только предлагать к удалению)
  - Модифицировать .env, credentials, secrets
  - Выполнять git push, git reset --hard
  - Устанавливать зависимости
  - Запускать произвольные shell-команды

  ОБЯЗАТЕЛЬНО:
  - Спрашивать подтверждение перед деструктивными операциями
  - Создавать бэкап перед массовыми изменениями
  - Логировать все изменения в state/
```

### Уровни доступа

| Уровень | Описание | Операции | Кто может использовать |
|---------|----------|----------|------------------------|
| **read-only** | Только чтение | Glob, Grep, Read | Все агенты |
| **safe-write** | Безопасная запись | + Write в разрешённые папки | Все агенты |
| **full** | Полный доступ | + Bash, удаление, все операции | **Только основной LLM** |

> **Агенты НЕ имеют full-доступа.** Все деструктивные операции выполняет только основной LLM.

### Удаление файлов агентом

Агенты НЕ МОГУТ удалять файлы. Вместо удаления:

1. **Пометить файл** — переименовать с префиксом `_old_`
2. **Записать в отчёт** — список файлов для удаления
3. **Основной LLM** — удалит после подтверждения пользователя

```yaml
prompt: |
  ## Удаление файлов

  ЗАПРЕЩЕНО: rm, удаление файлов

  Если нужно удалить файл:
  1. Переименовать: file.py → _old_file.py
  2. Записать в state/{agent-name}-deletions.json:
     {
       "files_to_delete": [
         {"original": "file.py", "renamed": "_old_file.py", "reason": "..."}
       ]
     }
  3. В отчёте указать: "Файлы помечены на удаление: ..."

  Основной LLM после ревью:
  - Удалит _old_* файлы
  - Или восстановит их (убрав префикс)
```

```yaml
# Пример ограничения
settings:
  access_level: safe-write
  allowed_paths:
    - src/
    - tests/
  forbidden_paths:
    - .env
    - .git/
    - node_modules/
  forbidden_operations:
    - delete
    - rm
    - git push
    - git reset
```
