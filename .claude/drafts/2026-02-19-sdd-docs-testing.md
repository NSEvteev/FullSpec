# docs/.system/testing.md — тестирование

Спецификация testing.md: секции, шаблон, пример. Стратегия тестирования: типы, структура файлов, мокирование, межсервисные тесты, тестовые данные, команды запуска.

## Контекст

**Задача:** Определить формат docs/.system/testing.md — документа со стратегией тестирования системы для LLM-разработчика.

**Источник:** `.claude/drafts/2026-02-19-sdd-chain-rethink.md` (строки 1834-2012)

**Связанные файлы:**
- `2026-02-19-sdd-structure.md` — общая структура и решения
- `2026-02-19-sdd-docs-technology.md` — standard-{tech}.md содержит per-tech тестовые паттерны (фреймворк, фикстуры, мокирование)
- `2026-02-19-sdd-docs-overview.md` — overview.md содержит сквозные потоки, которые превращаются в e2e-тесты

---

## Содержание

Как тестировать систему: типы тестов, где лежат файлы, что мокать на каком уровне, как запускать. Per-tech паттерны (pytest fixtures, mocking asyncpg) — в `standard-{tech}.md`.

### Секции docs/.system/testing.md

| # | Секция | Содержание | Зачем LLM-разработчику |
|---|--------|-----------|----------------------|
| 1 | **Типы тестов** | Unit, integration, e2e, smoke, load — что каждый проверяет, когда запускается, acceptance criteria | Понять какой тест писать для своего изменения |
| 2 | **Структура файлов** | Где лежат тесты per-service и системные, именование файлов/классов | Положить тест в правильное место |
| 3 | **Стратегия мокирования** | Что мокать на каждом уровне (unit: всё внешнее, integration: реальные БД, e2e: ничего) | Не мокать лишнее и не забыть нужное |
| 4 | **Межсервисные тесты** | Как тестировать взаимодействие 2+ сервисов, test containers, shared fixtures | Написать тест на кросс-сервисный сценарий |
| 5 | **Тестовые данные** | Подход к fixtures, factories, seed data. Как создавать, где хранить | Подготовить данные для теста |
| 6 | **Команды запуска** | make targets, флаги, фильтрация, coverage | Запустить нужные тесты |

### Шаблон: docs/.system/testing.md

`````markdown
# Тестирование

## Типы тестов

{Абзац: какие типы тестов используются в проекте и зачем. Общий принцип: unit для бизнес-логики, integration для хранилищ и межмодульных связей, e2e для полных сценариев через API.}

| Тип | Что проверяет | Scope | Внешние зависимости | Когда запускается |
|-----|--------------|-------|--------------------|--------------------|
| Unit | {что} | {scope} | {моки/реальные} | {когда} |
| Integration | {что} | {scope} | {моки/реальные} | {когда} |
| E2E | {что} | {scope} | {моки/реальные} | {когда} |
| Smoke | {что} | {scope} | {моки/реальные} | {когда} |
| Load | {что} | {scope} | {моки/реальные} | {когда} |

## Структура файлов

{Абзац: принцип организации — unit-тесты рядом с кодом сервиса, системные тесты в отдельной папке. Именование: `test_{module}.py`, классы `Test{Feature}`.}

```
/
├── src/{svc}/tests/          # Unit + integration тесты сервиса
│   ├── conftest.py           # Fixtures для этого сервиса
│   ├── test_{module}.py      # Тесты модуля
│   └── ...
├── tests/                    # Системные тесты (между сервисами)
│   ├── e2e/                  # End-to-end через API
│   │   ├── conftest.py       # Fixtures: запущенные сервисы, клиенты
│   │   └── test_{scenario}.py
│   ├── integration/          # Между 2+ сервисами, с реальными БД
│   │   └── test_{scenario}.py
│   ├── smoke/                # Быстрая проверка работоспособности
│   │   └── test_health.py
│   └── load/                 # Нагрузочное тестирование
│       └── {scenario}.py
```

## Стратегия мокирования

{Абзац: общий принцип — чем выше уровень теста, тем меньше моков. Unit мокает всё внешнее. Integration использует реальные хранилища. E2e не мокает ничего.}

| Уровень | БД | Message Broker | Другие сервисы | Shared-код | Внешние API |
|---------|-----|---------------|---------------|------------|-------------|
| Unit | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} |
| Integration | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} |
| E2E | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} | {мок/реальный} |

## Межсервисные тесты

{Абзац: как тестировать сценарии, в которых участвуют 2+ сервиса. Какой инструмент для поднятия сервисов (docker-compose, testcontainers). Как сквозные потоки из overview.md превращаются в тест-кейсы.}

{Абзац: паттерн написания межсервисного теста — подготовка данных, выполнение сценария, проверка результата в нескольких сервисах.}

## Тестовые данные

{Абзац: подход к тестовым данным — factories для генерации, fixtures для подготовки состояния БД. Где хранить seed data для e2e.}

**Принципы:**
- {принцип 1}
- {принцип 2}

## Команды запуска

| Команда | Что запускает | Окружение |
|---------|--------------|-----------|
| `{command}` | {описание} | {что нужно запущено} |
`````

### Пример: docs/.system/testing.md

`````markdown
# Тестирование

## Типы тестов

В проекте 5 типов тестов. Unit — основа: быстрые, изолированные, покрывают бизнес-логику. Integration — проверяют работу с реальными БД и брокером. E2E — полные сценарии через HTTP/WS API. Smoke — быстрая проверка что сервисы живы после деплоя. Load — нагрузочное тестирование перед релизом.

| Тип | Что проверяет | Scope | Внешние зависимости | Когда запускается |
|-----|--------------|-------|--------------------|--------------------|
| Unit | Бизнес-логика, валидация, маппинг, утилиты | Один модуль/функция | Все мокаются | При каждом коммите (pre-commit), в CI |
| Integration | Запросы к БД, AMQP pub/sub, repository-слой | Сервис + его хранилища | Реальные БД (Docker), мок других сервисов | В CI на каждый PR |
| E2E | Полные сценарии через API: регистрация → создание задачи → уведомление | Вся система | Всё реальное (docker-compose) | В CI перед merge в main |
| Smoke | Healthcheck всех сервисов, базовый CRUD | Вся система | Всё реальное | После деплоя на staging/prod |
| Load | Пропускная способность, latency под нагрузкой | Вся система | Всё реальное | Перед релизом, по запросу |

## Структура файлов

Unit и integration тесты живут внутри сервиса — рядом с кодом, который тестируют. Системные тесты (e2e, smoke, load) — в корневой `/tests/`, потому что они тестируют взаимодействие между сервисами. Именование: файл `test_{module}.py`, функция `test_{action}_{scenario}`, класс `Test{Feature}`.

```
/
├── src/notification/tests/       # Тесты notification-сервиса
│   ├── conftest.py               # Fixtures: тестовая БД, мок AMQP, фабрики
│   ├── test_repository.py        # Integration: запросы к PostgreSQL
│   ├── test_handlers.py          # Unit: обработка событий
│   ├── test_routes.py            # Unit: API endpoints (мок repository)
│   └── test_hub.py               # Unit: WebSocket Hub логика
├── src/auth/tests/               # Тесты auth-сервиса
│   ├── conftest.py
│   └── ...
├── tests/                        # Системные тесты
│   ├── e2e/
│   │   ├── conftest.py           # docker-compose up, HTTP-клиенты, WS-клиент
│   │   ├── test_registration_flow.py  # Регистрация → welcome-уведомление
│   │   ├── test_task_notification.py  # Создание задачи → уведомление назначенному
│   │   └── test_admin_role_change.py  # Смена роли → уведомление
│   ├── integration/
│   │   ├── conftest.py           # Отдельные сервисы + реальные БД
│   │   └── test_event_delivery.py # auth публикует → notification получает
│   ├── smoke/
│   │   └── test_health.py        # GET /health для каждого сервиса
│   └── load/
│       └── locustfile.py         # Locust-сценарии
```

## Стратегия мокирования

Принцип: чем выше уровень теста, тем меньше моков. Unit мокает всё за пределами тестируемого модуля. Integration поднимает реальные хранилища (PostgreSQL, Redis в Docker), но мокает другие сервисы. E2E не мокает ничего — все сервисы запущены реально.

| Уровень | БД | Message Broker | Другие сервисы | Shared-код | Внешние API |
|---------|-----|---------------|---------------|------------|-------------|
| Unit | Мок (in-memory) | Мок | Мок | Реальный | Мок |
| Integration | Реальный (Docker) | Реальный (Docker) | Мок (httpx mock) | Реальный | Мок |
| E2E | Реальный | Реальный | Реальный | Реальный | Мок (WireMock) |

Конкретные паттерны мокирования (pytest fixtures, AsyncMock, factory_boy) — в [standard-python.md](.technologies/standard-python.md#тестирование).

## Межсервисные тесты

Межсервисные сценарии описаны в [overview.md](overview.md#сквозные-потоки) — каждый сквозной поток должен быть покрыт e2e-тестом. Для поднятия всех сервисов используется `docker-compose -f docker-compose.test.yml up`, который запускает все сервисы + хранилища в изолированной сети.

Паттерн написания межсервисного теста:
1. **Arrange:** создать начальное состояние (зарегистрировать пользователя, получить JWT)
2. **Act:** выполнить основное действие (создать задачу через API task-сервиса)
3. **Assert:** проверить результат в нескольких сервисах (задача создана в task, уведомление появилось в notification, WS-сообщение отправлено)

При проверке асинхронных событий (AMQP → notification) — использовать polling с таймаутом: повторять проверку GET /notifications каждые 100ms до 5 секунд.

## Тестовые данные

Подход: factory_boy для генерации объектов, pytest fixtures для подготовки состояния БД. Seed data для e2e хранится в `tests/e2e/fixtures/`. Каждый тест создаёт свои данные и очищает после себя (транзакционный rollback для integration, truncate для e2e).

**Принципы:**
- Каждый тест независим — не зависит от порядка запуска и данных других тестов
- Фабрики генерируют минимальный валидный объект, тест переопределяет только нужные поля
- Для e2e: seed data создаётся через API (не напрямую в БД), чтобы проверить полный цикл
- Чувствительные данные (JWT ключи) — фиксированные тестовые значения в `conftest.py`, не из .env

## Команды запуска

Make-таргеты для тестирования определены в [Makefile](/Makefile) (SSOT). Полный список: `make help`.

**Прямой вызов pytest** (для запуска конкретных тестов по фильтру):

```bash
pytest src/{svc}/tests/ -k "test_{pattern}"        # Тесты одного сервиса по фильтру
pytest tests/e2e/ -k "test_registration"            # Конкретный e2e-сценарий
pytest src/notification/tests/test_handlers.py -v    # Один файл, подробный вывод
```
`````

---

## Аудит старых документов

| Старый документ | Что переиспользовать |
|-----------------|---------------------|
| `specs/tests/README.md` | Текущая структура (пустой, только README) — ничего для переиспользования |
