# Issue как самодостаточная задача для разработки

Переработка флоу создания Issues — Issue как полностью самодостаточный "промт" для LLM-агента: единая навигация по документам (спеки, .system/, per-tech стандарты), явное задание, практический контекст.

## Оглавление

- [Контекст](#контекст)
- [Содержание](#содержание)
  - [1. Проблема: Issue #56](#1-проблема-issue-56)
  - [2. Issue как промт для LLM-агента](#2-issue-как-промт-для-llm-агента)
  - [3. Предлагаемая структура body](#3-предлагаемая-структура-body)
  - [4. Секция "Документы для изучения"](#4-секция-документы-для-изучения)
  - [5. Секция "Задание"](#5-секция-задание)
  - [6. Секция "Практический контекст"](#6-секция-практический-контекст)
  - [7. Фильтрация документов по релевантности](#7-фильтрация-документов-по-релевантности)
  - [8. Алгоритм сбора контекста при /dev-create](#8-алгоритм-сбора-контекста-при-dev-create)
  - [9. Изменения в стандартах](#9-изменения-в-стандартах)
  - [10. Миграция существующих Issues](#10-миграция-существующих-issues)
- [Решения](#решения)
- [Открытые вопросы](#открытые-вопросы)
- [Затронутые файлы](#затронутые-файлы)
- [Tasklist](#tasklist)

---

## Контекст

**Задача:** Issue #56 (и все остальные #42-#61) содержат минимальный body — указатель на документы, а не самодостаточную задачу. LLM-агент не может разработать по такому Issue без навигации по 5+ файлам.
**Почему создан:** Каждый Issue будет выполняться отдельным dev-agent (субагентом). Субагенты НЕ имеют общего контекста с основной сессией — каждый Issue должен быть полностью самодостаточным "промтом": все пути, все инструкции, все зависимости.
**Связанные файлы:**
- `.github/.instructions/issues/standard-issue.md` — стандарт Issues (§ 4 Body)
- `.github/.instructions/issues/create-issue.md` — воркфлоу создания Issue
- `.github/.instructions/development/create-development.md` — воркфлоу /dev-create
- `specs/.instructions/analysis/plan-dev/standard-plan-dev.md` — стандарт Plan Dev
- `specs/.instructions/docs/technology/standard-technology.md` — стандарт per-tech
- `specs/.instructions/docs/system/standard-system.md` — стандарт .system/ файлов
- `specs/docs/.technologies/` — per-tech стандарты кодирования (9 файлов)
- `specs/docs/.system/` — кросс-сервисные документы (4 файла)
- `specs/docs/{svc}.md` — per-service документы

**Почему самодостаточность критична:** Субагенты (dev-agent) запускаются через Agent tool и работают в ИЗОЛИРОВАННОМ контексте. Они НЕ имеют доступа к TaskList основной сессии, НЕ видят историю разговора. Единственный вход — текст промта (Issue body). Если в Issue нет ссылки на документ — агент его не прочитает.

**Пересечение:** Этот черновик меняет те же файлы, что `2026-03-02-expand-type-labels.md` (standard-issue.md, create-issue.md, create-development.md, validate-issue.py). Рекомендуемый порядок: сначала TYPE-метки, потом self-sufficiency.

---

## Содержание

### 1. Проблема: Issue #56

[Issue #56](https://github.com/NSEvteev/project_template/issues/56) — "Реализовать аутентификацию LoginPage и authStore (TASK-15)".

Что видит dev-agent:

```markdown
## Описание

Zustand authStore (token, user, login, logout), LoginPage с формой email/password,
ProtectedRoute, роутинг /login + / (protected).

**Сложность:** 5/10 | **Wave:** 2 (frontend) | **TC:** TC-28, TC-29, TC-30
**Зависит от:** #55

## Связанная документация

- Plan Dev — specs/analysis/0001-task-dashboard/plan-dev.md (TASK-15)
- Design SVC-3 § 4, § 5, INT-2 — specs/analysis/0001-task-dashboard/design.md

## Критерии готовности

- [ ] 15.1. authStore.ts (Zustand): token, user, login(), logout()
...
```

Проблемы:
1. **Описание** — одна строка-перечисление, нет контекста ЧТО и ЗАЧЕМ
2. **Нет навигации** — "Design SVC-3 § 4, § 5" — агент не знает, что именно искать в этих секциях
3. **Нет системных документов** — агент не знает про conventions.md (формат ошибок, JWT), overview.md (архитектура)
4. **Нет технологий** — агент не знает, какие per-tech стандарты читать (React, TypeScript)
5. **Нет задания** — чек-лист "15.1. authStore.ts" — это РЕЗУЛЬТАТ, а не ИНСТРУКЦИЯ
6. **Нет практического контекста** — какие файлы уже существуют, что экспортирует #55, как запустить

### 2. Issue как промт для LLM-агента

Ключевая идея: Issue = **полный промт** для dev-agent. Агент получает Issue и должен выполнить задачу БЕЗ дополнительного контекста.

**Структура промта (5 секций):**

| # | Секция | Назначение | Аналогия в промтинге |
|---|--------|-----------|---------------------|
| 1 | Описание задачи | ЧТО, ЗАЧЕМ, зависимости | System prompt — контекст задачи |
| 2 | Документы для изучения | ВСЕ документы: спеки, .system/, per-tech | Tool calls — чтение конкретных файлов |
| 3 | Задание | ЧТО ИМЕННО сделать (пошагово) | User prompt — конкретная инструкция |
| 4 | Критерии готовности | КАК ПРОВЕРИТЬ результат | Acceptance criteria — ссылка на Plan Test |
| 5 | Практический контекст | Существующий код, зависимости, запуск | Environment — где и как работать |

Каждая секция обязывает агента ПРОЧИТАТЬ конкретные файлы. Issue — не справочник, а МАРШРУТ.

**Почему 5, а не 8:** Критический анализ показал, что 8 секций содержат дублирование ("Связанная документация" = копия "Документов"), пустые по существу секции ("Зависимости" = одна строка) и искусственное разделение ("Системные документы" и "Технологии" — это такие же документы для изучения). 5 секций покрывают всё без дублирования.

### 3. Предлагаемая структура body

```markdown
## Описание задачи

Реализовать аутентификацию на фронтенде: Zustand-стор для хранения JWT-токена
и данных пользователя, страницу логина с формой email/password, компонент
ProtectedRoute для защиты маршрутов и роутинг приложения.

Это часть frontend-сервиса, который общается с auth-сервисом через REST API.
После реализации пользователь сможет войти в систему и получить доступ к дашборду.

**Сложность:** 5/10 | **Сервис:** frontend | **Wave:** 2
**Зависит от:** #55 (API-слой и TanStack Query хуки) — ДОЛЖЕН быть готов.

## Документы для изучения

Перед началом работы ОБЯЗАТЕЛЬНО прочитай документы из таблицы ниже.
Каждый документ содержит информацию, критичную для реализации задачи.

| # | Документ | Путь | Что искать |
|---|----------|------|-----------|
| 1 | Архитектура системы | specs/docs/.system/overview.md | Карта сервисов, как frontend связан с auth, сквозной поток логина |
| 2 | Конвенции API | specs/docs/.system/conventions.md | Формат ответов, формат ошибок, структура JWT claims, Bearer |
| 3 | Design SVC-3 § 4 (API) | specs/analysis/0001-task-dashboard/design.md | Контракт POST /api/v1/auth/login — request/response, status codes 200/401 |
| 4 | Design SVC-3 § 5 (Tech Stack) | (тот же файл) | Zustand 5.x для state, React Router 7 для роутинга, JWT в localStorage |
| 5 | Design INT-2 (Интеграция) | (тот же файл) | Flow frontend ↔ auth: как frontend вызывает auth API, где хранится токен |
| 6 | Сервисная документация | specs/docs/frontend.md | Code Map (§ 5), структура проекта, точки входа, зависимости |
| 7 | React стандарт | specs/docs/.technologies/standard-react.md | Структура компонентов, хуки, паттерны — ОБЯЗАТЕЛЬНО следуй при написании кода |
| 8 | TypeScript стандарт | specs/docs/.technologies/standard-typescript.md | Типизация, именование, импорты — ОБЯЗАТЕЛЬНО следуй при написании кода |
| 9 | Тестирование | specs/docs/.system/testing.md | Стратегия тестов, мокирование, структура тест-файлов — читай перед написанием тестов |
| 10 | Plan Test (тест-кейсы) | specs/analysis/0001-task-dashboard/plan-test.md | TC-28, TC-29, TC-30 — сценарии тестов: входные данные, шаги, ожидаемый результат. Напиши тесты по ним |

## Задание

В соответствии с описанием frontend-сервиса в specs/docs/frontend.md,
на основании проектирования в Design SVC-3 (specs/analysis/0001-task-dashboard/design.md)
и критериев приёмки из Plan Test TC-28..TC-30 (specs/analysis/0001-task-dashboard/plan-test.md),
реализуй аутентификацию frontend-приложения:

1. **Создай `src/frontend/stores/authStore.ts`** — Zustand стор.
   Поля: `token` (string | null), `user` (User | null).
   Методы: `login(email, password)` — вызывает POST /api/v1/auth/login
   через API-слой из #55, сохраняет JWT в localStorage и обновляет стор.
   `logout()` — очищает token и user, удаляет из localStorage.
   Формат API-ответа: см. Design SVC-3 § 4. Формат ошибок: см. conventions.md.

2. **Создай `src/frontend/pages/LoginPage.tsx`** — страница логина.
   Форма с полями email и password. При submit → `authStore.login()`.
   При ошибке 401 → показать сообщение "Неверный email или пароль" (TC-29).
   При успехе → редирект на /. Следуй паттернам из standard-react.md.

3. **Создай `src/frontend/components/ProtectedRoute.tsx`** — обёртка маршрута.
   Если `authStore.token` === null → Navigate to /login (TC-30).
   Используй React Router 7 Navigate (см. Design SVC-3 § 5).

4. **Обнови `src/frontend/App.tsx`** — добавь роутинг:
   - /login — публичный маршрут (LoginPage)
   - / — защищённый через ProtectedRoute (Dashboard)
   Успешный логин → редирект на / → дашборд виден (TC-28).

5. **Напиши тесты** для реализованного кода.
   Тест-кейсы описаны в Plan Test: TC-28, TC-29, TC-30.
   Прочитай каждый TC полностью — он содержит сценарий, входные данные и ожидаемый результат.
   Структура тестов и стратегия мокирования: см. testing.md в таблице "Документы для изучения".

## Критерии готовности

Тест-кейсы приёмки (TC) описаны в Plan Test:
specs/analysis/0001-task-dashboard/plan-test.md

TC-28, TC-29, TC-30 — это acceptance-сценарии, по которым будет проверяться твоя работа.
Прочитай их ПОЛНОСТЬЮ перед началом работы. Каждый TC содержит: сценарий, входные данные,
ожидаемый результат. Напиши тесты, покрывающие эти сценарии.

Подзадачи (отмечай по мере выполнения):
- [ ] 15.1. authStore.ts: token, user, login(), logout()
- [ ] 15.2. LoginPage.tsx: форма + обработка ошибок (401 → сообщение)
- [ ] 15.3. Интеграция с API-слоем → localStorage + authStore
- [ ] 15.4. ProtectedRoute: редирект при отсутствии JWT
- [ ] 15.5. App.tsx: роутинг /login (публичный) + / (protected)
- [ ] 15.6. Тесты: TC-28, TC-29, TC-30

## Практический контекст

**Существующий код:**
src/frontend/ содержит: App.tsx, main.tsx, api/ (из #55), components/, pages/.
Точка входа: src/frontend/main.tsx → App.tsx.

**Зависимость #55 экспортирует:**
`src/frontend/api/auth.ts` → `authApi.login(email, password): Promise<LoginResponse>`
Использовать в authStore для вызова API.

**Как запустить:** `make dev` → http://localhost:3000
**Как проверить:** `cd src/frontend && npm test` — unit-тесты
```

### 4. Секция "Документы для изучения"

**Единая таблица.** ВСЕ документы, которые агент должен прочитать, в одном месте: .system/ файлы, Design секции, per-tech стандарты, сервисная документация, Plan Test.

**Формат:** Таблица с 4 колонками: #, Документ, Путь, Что искать.

**Источники данных (при `/dev-create`):**

| # | Источник | Что генерировать |
|---|----------|-----------------|
| 1 | Фильтр .system/ (§ 7) | Релевантные .system/ файлы с описанием "что искать" |
| 2 | `Источник: SVC-N § K` | Design SVC-N § K — краткое описание, что искать в секции |
| 3 | SVC-N → `specs/docs/{svc}.md` | Сервисная документация — "Code Map (§ 5), структура, точки входа" |
| 4 | `{svc}.md` → Tech Stack | Per-tech стандарты, отфильтрованные по задаче — "ОБЯЗАТЕЛЬНО следуй" |
| 5 | `TC: TC-N, TC-M` | Plan Test (тест-кейсы) — "сценарии тестов: входные данные, шаги, ожидаемый результат. Напиши тесты по ним" |

**Не копировать данные:** Issue содержит ПУТИ и ОПИСАНИЯ — что искать. LLM сам прочитает через Read tool. Документы могут обновиться при CONFLICT, а Issue — нет. Ссылки важнее snapshot-ов.

**Per-tech стандарты в таблице:** Технологии — это такие же документы для изучения, как Design или conventions.md. Мы специально пишем per-tech стандарты, чтобы агент им следовал. Отдельная секция — искусственное разделение. В колонке "Что искать" указать: "ОБЯЗАТЕЛЬНО следуй при написании кода".

**Алгоритм определения per-tech (при `/dev-create`):**
1. Из поля `Источник` определить SVC-N → `specs/docs/{svc}.md`
2. Прочитать `{svc}.md` § Code Map → таблица Tech Stack
3. Для каждой технологии найти `specs/docs/.technologies/standard-{tech}.md`
4. Отфильтровать только релевантные для данной задачи
5. Добавить в единую таблицу "Документы для изучения"

### 5. Секция "Задание"

Ядро промта — явная пошаговая инструкция человеческим языком.

**Формат вступления (шаблон):**
```
В соответствии с описанием {сервис}-сервиса в specs/docs/{svc}.md,
на основании проектирования в Design SVC-N (specs/analysis/NNNN-{topic}/design.md)
и критериев приёмки из Plan Test TC-N..TC-M (specs/analysis/NNNN-{topic}/plan-test.md),
реализуй {описание задачи}:
```

**Принципы каждого пункта:**
- Начало: **действие** + **путь к файлу** (создай `src/.../file.ts`)
- Контекст: что должен делать файл/модуль
- Ссылки: "Формат ответа: см. Design SVC-N § K", "Формат ошибок: см. conventions.md"
- Acceptance: "(TC-29)" — привязка к конкретному тест-кейсу
- Зависимости: "через API-слой из #55"
- Инструкции: "Следуй паттернам из standard-react.md"

**Тесты — обязательный пункт:** Последний пункт "Задания" ВСЕГДА — "Напиши тесты". Поле `TC: TC-N` в TASK-N указывает тест-кейсы из Plan Test — это сценарии, по которым агент ДОЛЖЕН написать тесты. Если TC не указаны — пункт про тесты опускается.

**Откуда генерировать:** Подзадачи TASK-N (N.1, N.2, ...) + контекст из Design SVC-N §§. Каждая подзадача разворачивается в полную инструкцию. Последний пункт — тесты из TC.

### 6. Секция "Практический контекст"

**Новая секция.** Информация, которой нет в спецификациях, но без которой агент не может начать работу.

**Три блока:**

| Блок | Откуда берётся | Зачем |
|------|---------------|-------|
| Существующий код | `ls src/{svc}/` при `/dev-create` | Агент знает, что уже есть, не создаёт конфликтующие файлы |
| Зависимость #N экспортирует | Из Issue зависимости → его body/подзадачи | Агент знает конкретный импорт, а не абстрактный "API-слой" |
| Как запустить/проверить | Из Makefile / {svc}.md § Dev | Агент может запустить и проверить результат |

**Снимок существующего кода:** При `/dev-create` выполнить `ls src/{svc}/` и включить результат в Issue. Это snapshot — при CONFLICT может устареть, но для начала работы достаточно. Агент прочитает актуальную структуру через Glob/Read.

**Зависимости: что КОНКРЕТНО экспортирует #N:** Не "API-слой из #55", а "src/frontend/api/auth.ts → authApi.login(email, password): Promise\<LoginResponse\>". Агент должен знать ИНТЕРФЕЙС зависимости: файл, функция, сигнатура.

### 7. Фильтрация документов по релевантности

**Проблема:** Не все .system/ файлы нужны для каждой задачи. infrastructure.md (порты, БД, Docker) нерелевантен для фронтенд-стора. Кроме того, infrastructure.md содержит визуализацию (пример из стандарта, не реальные данные проекта) — включение может ввести агента в заблуждение.

**testing.md:** Включается ВСЕГДА, если TASK-N имеет поле `TC:` (тест-кейсы). Агент должен знать стратегию тестирования, мокирование и структуру тест-файлов. Если `TC:` отсутствует — testing.md опускается.

**Фильтр по типу сервиса из `Источник: SVC-N`:**

| Тип задачи | overview.md | conventions.md | infrastructure.md | testing.md |
|------------|:-----------:|:--------------:|:-----------------:|:----------:|
| frontend | + | + (JWT, ошибки) | — | + (если есть TC) |
| backend | + | + | + (порты, БД) | + (если есть TC) |
| infra (без SVC) | + | — | + (PRIMARY) | + (если есть TC) |
| test | + | + | + | + (PRIMARY) |

**Определение типа:** Из `Источник: SVC-N` → `specs/docs/{svc}.md` → § Code Map → Tech Stack. Если React/Vue → frontend. Если Express/Fastify/NestJS → backend. Если нет SVC → infra.

**Дополнительный фильтр по подзадачам:**
- Подзадача содержит "тест" / "test" → добавить testing.md
- Подзадача содержит "порт" / "Docker" / "env" → добавить infrastructure.md
- Подзадача содержит "API" / "endpoint" / "ошибк" → добавить conventions.md

**Per-tech стандарты:** Фильтровать по Tech Stack сервиса. Из 9 доступных стандартов для frontend-задачи нужны только React + TypeScript (2 из 9).

### 8. Алгоритм сбора контекста при /dev-create

Для каждого TASK-N из Plan Dev:

```
1. Прочитать TASK-N: Источник (SVC-N §§), TC (TC-N), Зависимости, подзадачи
2. Определить сервис: SVC-N → {svc} → specs/docs/{svc}.md
3. Определить тип сервиса: {svc}.md → Tech Stack → frontend/backend/infra (§ 7)
4. Определить наличие тестов: есть ли поле TC: в TASK-N? Если да → агент ДОЛЖЕН писать тесты

--- Секция "Описание задачи" ---
5. Описание: 2-3 предложения о ЧТО и ЗАЧЕМ (заголовок TASK-N + контекст Design)
   + сложность, сервис, волна, зависимости (Issues + что от них нужно)

--- Секция "Документы для изучения" ---
6. .system/ файлы: отфильтровать по типу сервиса (§ 7) → добавить в таблицу
   Если есть TC → ОБЯЗАТЕЛЬНО включить testing.md (стратегия, мокирование, структура)
7. Design: из Источника → Design SVC-N § K с описанием "что искать"
8. Сервисная документация: specs/docs/{svc}.md — "Code Map, структура, точки входа"
9. Per-tech стандарты: {svc}.md → Tech Stack → standard-{tech}.md (отфильтрованные)
10. Plan Test: TC-N — "сценарии тестов: входные данные, шаги, ожидаемый результат.
    Напиши тесты по ним"

--- Секция "Задание" ---
11. Вступление (шаблон из § 5) + подзадачи → развёрнутые инструкции с ссылками
12. Если есть TC → добавить последний пункт: "Напиши тесты для TC-N..TC-M"
    со ссылкой на Plan Test и testing.md

--- Секция "Критерии готовности" ---
13. Ссылка на Plan Test + список TC-N + подзадачи как чек-лист
    + подзадача "Тесты: TC-N, TC-M" (если есть TC)

--- Секция "Практический контекст" ---
14. Существующий код: ls src/{svc}/ → snapshot структуры
15. Зависимости: для каждого Issues-зависимости → файл + экспорт + сигнатура
16. Как запустить: из Makefile / {svc}.md § Dev

17. Собрать body: 5 секций
```

### 9. Изменения в стандартах

**standard-issue.md** — § 4 Body:
- Обновить структуру body для Issues из Plan Dev (5 секций вместо 3):
  1. Описание задачи (2-3 предложения, контекст, зависимости)
  2. Документы для изучения (единая таблица: .system/ + Design + per-tech + {svc}.md + Plan Test)
  3. Задание (пошаговые инструкции с ссылками на Design/TC/conventions)
  4. Критерии готовности (ссылка на Plan Test + TC-N + подзадачи)
  5. Практический контекст (существующий код, экспорты зависимостей, запуск)
- Секции 2-5 опциональны для ручных Issues, обязательны при создании из Plan Dev
- Bump версии + `/migration-create`

**create-issue.md** — Шаг 4 "Заполнить body":
- Добавить алгоритм сбора контекста (§ 8 этого драфта, 17 шагов)
- Две ветки: "Из Plan Dev" (полный алгоритм) vs "Ручной Issue" (3 секции, как сейчас)
- Обновить примеры — добавить пример body из Plan Dev (из § 3 этого драфта)

**create-development.md** — Шаг 3 "Создать GitHub Issues":
- Вместо краткого body → полный алгоритм из § 8: для каждого TASK-N собирать body
- Добавить пример сгенерированного body (из § 3)
- Описание Issue: 2-3 предложения (НЕ одна строка-перечисление)

**validate-issue.py**:
- Warning (не error) при отсутствии секций "Документы для изучения", "Задание", "Критерии готовности", "Практический контекст" для Issues из Plan Dev

**standard-plan-dev.md** — поле `Источник`:
- Усилить правило: Источник ДОЛЖЕН быть достаточно точным (SVC-N § {номер секции})
- Добавить: "Используется при /dev-create для генерации навигации в Issue body"
- Bump версии + `/migration-create` (если ещё не bump от TYPE-драфта)

**standard-issue-template.md:**
- Документировать: "Batch-created Issues (из Plan Dev) содержат 5 секций вместо 3. Шаблоны (task.yml и др.) НЕ меняются — дополнительные секции генерируются при batch-создании через `gh issue create`."

### 10. Миграция существующих Issues

Issues #42-#61 — обогатить body полной структурой:
1. Для каждого TASK-N прочитать: Источник, TC, подзадачи, зависимости
2. Определить тип сервиса (frontend/backend/infra) → фильтр .system/ файлов
3. Собрать единую таблицу "Документы для изучения" (.system/ + Design + per-tech + {svc}.md + Plan Test)
4. Развернуть подзадачи в секцию "Задание" с ссылками на Design/TC
5. Критерии: ссылка на Plan Test + TC-N + подзадачи
6. Практический контекст: ls src/{svc}/, экспорты зависимостей, запуск
7. Собрать body по 5-секционной структуре § 3
8. `gh issue edit N --body "..."`

Автоматизация: LLM читает plan-dev.md → для каждого TASK-N собирает контекст → генерирует body.

---

## Решения

- **Issue = полный промт** — агент получает Issue и работает автономно. Никакого "общего контекста" — субагенты изолированы.
- **Навигация, а не копирование** — Issue содержит ПУТИ к документам + описание "что искать". LLM читает через Read tool. Документы — SSOT, Issue — маршрут.
- **Единая таблица документов** — .system/, Design, per-tech стандарты, Plan Test — в ОДНОЙ таблице "Документы для изучения". Нет искусственного разделения: per-tech стандарты — такие же документы, как и Design.
- **Фильтрация по релевантности** — НЕ все 4 .system/ файла всегда. Фильтр по типу сервиса (frontend/backend/infra) + дополнительный фильтр по подзадачам. Исключает нерелевантные документы (infrastructure.md для фронтенд-стора).
- **Практический контекст** — то, чего нет в спецификациях: существующий код, конкретные экспорты зависимостей, команды запуска. Без этого агент не может начать работу.
- **Задание-промт** — пошаговая инструкция с ссылками на Design/TC/conventions. Формат: "создай X, который делает Y (см. Design § Z, conventions.md)".
- **Критерии через Plan Test** — ссылка на SSOT (Plan Test) + конкретные TC-N, а не дублирование.
- **5 секций вместо 8** — убраны дубли ("Связанная документация" = копия "Документов"), пустые секции ("Зависимости" = одна строка → в "Описание"), искусственные разделения ("Системные документы" и "Технологии" → в единую таблицу).
- **Issue templates не меняются** — секции генерируются при batch-создании из Plan Dev, не заполняются вручную.

---

## Открытые вопросы

1. **INFRA-задачи** — TASK-1 (монорепо, docker-compose) не привязан к SVC-N. Источник документов: Design "Выбор технологий" + infrastructure.md (PRIMARY). Алгоритм фильтрации (§ 7) — ветка "infra (без SVC)".
2. **Порядок с TYPE-драфтом** — оба драфта меняют одни файлы. Рекомендация: сначала TYPE-метки, потом self-sufficiency.
3. **Snapshot существующего кода** — при CONFLICT структура src/ может измениться. Issue содержит snapshot на момент создания. Агент может прочитать актуальную структуру через Glob.

---

## Затронутые файлы

| Файл | Изменение |
|------|-----------|
| `.github/.instructions/issues/standard-issue.md` | § 4 Body — 5 секций вместо 3, bump + миграция |
| `.github/.instructions/issues/create-issue.md` | Шаг 4 — алгоритм сбора контекста (17 шагов) |
| `.github/.instructions/development/create-development.md` | Шаг 3 — полный body с навигацией |
| `.github/.instructions/issues/issue-templates/standard-issue-template.md` | Документировать batch-created 5 секций |
| `specs/.instructions/analysis/plan-dev/standard-plan-dev.md` | Правило Источник — используется для навигации |
| `.github/.instructions/.scripts/validate-issue.py` | Warning: "Документы для изучения", "Задание", "Критерии готовности", "Практический контекст" |
| Issues #42-#61 | Миграция body — полная 5-секционная структура |

---

## Tasklist

Задачи для исполнения через TaskCreate. Порядок строгий — зависимости указаны в blockedBy.

TASK 1: Обновить standard-issue.md — 5-секционная структура body
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "9. Изменения в стандартах")
    Обновить `.github/.instructions/issues/standard-issue.md`:
    - § 4 Body — определить 5 секций для Issues из Plan Dev:
      1. Описание задачи (2-3 предложения, зависимости)
      2. Документы для изучения (единая таблица: .system/ + Design + per-tech + {svc}.md + Plan Test)
      3. Задание (пошаговые инструкции с ссылками на Design/TC/conventions)
      4. Критерии готовности (ссылка на Plan Test + TC-N + подзадачи)
      5. Практический контекст (существующий код, экспорты, запуск)
    - Секции 2-5 опциональны для ручных Issues, обязательны из Plan Dev
    - Обновить "Полный пример body" (пример из § 3 драфта)
    - Bump версии
    Запустить `/migration-create` после изменения.
  activeForm: Обновление standard-issue.md

TASK 2: Обновить create-issue.md — алгоритм сбора контекста
  blockedBy: [1]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "8. Алгоритм сбора контекста")
    Обновить `.github/.instructions/issues/create-issue.md`:
    - Шаг 4 "Заполнить body" — две ветки:
      a) "Из Plan Dev": 17-шаговый алгоритм (§ 8 драфта) с фильтрацией документов (§ 7)
      b) "Ручной Issue": как сейчас (3 секции)
    - Обновить примеры — добавить пример body из Plan Dev (§ 3 драфта)
  activeForm: Обновление create-issue.md

TASK 3: Обновить create-development.md — генерация body при /dev-create
  blockedBy: [2]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "8. Алгоритм сбора контекста")
    Обновить `.github/.instructions/development/create-development.md`:
    - Шаг 3: для каждого TASK-N собирать полный body (17-шаговый алгоритм)
    - Фильтрация .system/ по типу сервиса (§ 7)
    - Per-tech стандарты из {svc}.md → Tech Stack
    - "Практический контекст": ls src/{svc}/, экспорты зависимостей, запуск
    - Добавить пример сгенерированного body (§ 3 драфта)
  activeForm: Обновление create-development.md

TASK 4: Документировать batch-created секции в standard-issue-template.md
  blockedBy: [1]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "9. Изменения в стандартах")
    Обновить standard-issue-template.md:
    - "Batch-created Issues (из Plan Dev) содержат 5 секций вместо 3"
    - Список дополнительных секций с описанием
    - Шаблоны НЕ меняются — секции генерируются через `gh issue create`
  activeForm: Обновление standard-issue-template.md

TASK 5: Обновить standard-plan-dev.md — правило для поля Источник
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "9. Изменения в стандартах")
    Усилить правило Источник: SVC-N § {номер секции}.
    - "Используется при /dev-create для генерации навигации в Issue body"
    - Примеры: хорошо (SVC-3 § 4, SVC-3 § 5) vs плохо (SVC-3)
    Если bump — запустить `/migration-create`.
  activeForm: Обновление правила Источник

TASK 6: Обновить validate-issue.py — проверка 4 секций
  blockedBy: [1]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "9. Изменения в стандартах")
    Добавить проверку для Plan Dev Issues (title содержит TASK-N):
    - Warning при отсутствии: "## Документы для изучения", "## Задание",
      "## Критерии готовности", "## Практический контекст"
    - Warning (не error) — обратная совместимость
  activeForm: Обновление validate-issue.py

TASK 7: Миграция существующих Issues #42-#61
  blockedBy: [3]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (секция "10. Миграция")
    Для каждого из 20 Issues собрать body по 5-секционной структуре:
    1. Описание задачи (2-3 предложения, зависимости)
    2. Документы для изучения (единая таблица: .system/ фильтрованные + Design + per-tech + {svc}.md + Plan Test)
    3. Задание (развёрнутые инструкции с ссылками)
    4. Критерии готовности (ссылка Plan Test + TC-N + подзадачи)
    5. Практический контекст (ls src/{svc}/, экспорты, запуск)
    Обновить: `gh issue edit N --body "..."`
  activeForm: Миграция body Issues

TASK 8: Валидация
  blockedBy: [1, 2, 3, 4, 5, 6, 7]
  description: >
    Драфт: .claude/drafts/2026-03-02-issue-self-sufficiency.md (весь документ)
    Валидация:
    1. `/issue-validate --all` — все Issues
    2. `/migration-validate` — дрифт стандартов
    3. Визуальная: Issue #56 → body содержит все 5 секций
    4. Тест самодостаточности: прочитать ТОЛЬКО Issue #56, не открывая specs/ — можно ли начать работу?
  activeForm: Валидация изменений
